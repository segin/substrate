#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    (void)argv;
    if (argc < 2) return 1;
    // size [file]
    printf("size: printing section sizes (stub)\n");
    return 0;
}

