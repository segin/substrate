# /usr/bin/strings is provided by GNU binutils (contrib/binutils
# stage 2); the dist-toolchain overlay installs it into the
# rootfs.  This directory has no in-tree build of its own.
.PHONY: all clean install depend
all clean install depend:
	@:
