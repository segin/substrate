# Requirements to Test Matrix

| Req ID | Requirement Description | Test Case | Status |
|--------|-------------------------|-----------|--------|
| R1 | PAX created archives SHALL use extended headers for overflows | `tests/pax/test_pax_header.sh` | Stubbed |
| R2 | Read legacy ustar/cpio and convert to pax | `tests/pax/test_compat.sh` | Implemented (Basic) |
| R3 | Substitution rules SHALL be applied deterministically | `tests/pax/test_subs.sh` | Stubbed |
| F1 | Mode -r (Read) | `pax -r -f archive.tar` | Implemented |
| F2 | Mode -w (Write) | `pax -w -f archive.tar file` | Implemented |
| F3 | Mode -rw (Copy) | `pax -rw src dst` | Implemented |
| F4 | Format ustar | `pax -w -x ustar` | Implemented |
| F5 | Format cpio | `pax -w -x cpio` | Implemented |
