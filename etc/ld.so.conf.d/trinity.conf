# Trinity Desktop Environment — installs under /opt/trinity to coexist
# with KDE.  Its shared libraries and tdeinit_* wrapper libraries are
# referenced by bare soname with no RPATH, so the directory must be on
# the loader search path.
/opt/trinity/lib
