/* <util.h> — NetBSD-historical pty helpers.  Same content as <pty.h>;
 * many BSD-derived ports (OpenSSH's sshpty.c included) reach for
 * <util.h> when configure detects HAVE_UTIL_H. */
#ifndef _UTIL_H
#define _UTIL_H
#include <pty.h>
#endif
