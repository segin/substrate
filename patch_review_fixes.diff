--- usr.bin/ld/ld.c
+++ usr.bin/ld/ld.c
@@ -3230,8 +3230,10 @@
         close(pfd[0]);
     }

-    if (waitpid(pid, &status, 0) == -1) {
-        return -1;
+    while (waitpid(pid, &status, 0) == -1) {
+        if (errno != EINTR) {
+            return -1;
+        }
     }
     if (!WIFEXITED(status) || WEXITSTATUS(status) != 0) {
         out[0] = '\0';
@@ -3331,8 +3333,10 @@
         _exit(127);
     }

-    if (waitpid(pid, &status, 0) == -1) {
-        fprintf(stderr, "ld: waitpid failed for plugin handshake\n");
-        return -1;
+    while (waitpid(pid, &status, 0) == -1) {
+        if (errno != EINTR) {
+            fprintf(stderr, "ld: waitpid failed for plugin handshake\n");
+            return -1;
+        }
     }

@@ -3351,7 +3355,8 @@
     size_t i;
     int status;
     int argc;
     char **argv;
+    char **opts;

     if (out_path == NULL || out_path_sz == 0) {
         return -1;
@@ -3375,7 +3380,8 @@
             close(pfd[1]);
         }

-        argc = 3 + ctx->plugin_opt_count * 2;
+        argc = 3 + ctx->plugin_opt_count;
+        opts = (char **)malloc(ctx->plugin_opt_count * sizeof(char *));
         argv = (char **)malloc((argc + 1) * sizeof(char *));
-        if (argv == NULL) {
+        if (argv == NULL || (ctx->plugin_opt_count > 0 && opts == NULL)) {
             _exit(127);
         }

@@ -3384,8 +3390,13 @@
         argv[2] = (char *)in_path;

         for (i = 0; i < ctx->plugin_opt_count; ++i) {
-            argv[3 + i * 2] = (char *)"--plugin-opt";
-            argv[3 + i * 2 + 1] = (char *)ctx->plugin_opts[i];
+            size_t len = strlen("--plugin-opt=") + strlen(ctx->plugin_opts[i]) + 1;
+            opts[i] = (char *)malloc(len);
+            if (opts[i] == NULL) {
+                _exit(127);
+            }
+            snprintf(opts[i], len, "--plugin-opt=%s", ctx->plugin_opts[i]);
+            argv[3 + i] = opts[i];
         }
         argv[argc] = NULL;

@@ -3402,8 +3413,10 @@
         close(pfd[0]);
     }

-    if (waitpid(pid, &status, 0) == -1) {
-        out_path[0] = '\0';
-        return -1;
+    while (waitpid(pid, &status, 0) == -1) {
+        if (errno != EINTR) {
+            out_path[0] = '\0';
+            return -1;
+        }
     }
