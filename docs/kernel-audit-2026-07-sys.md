# Substrate kernel audit — sys/ (2026-07-22)

Multi-agent audit: 21 subsystem auditors read the real source, each finding then adversarially verified by an independent skeptic. 90 raw findings → **87 survived** (80 CONFIRMED, 7 PLAUSIBLE), 3 refuted.

| Severity | Count |
|---|---|
| Critical | 4 |
| High | 27 |
| Medium | 23 |
| Low | 33 |


## CRITICAL

### ptrace PEEK/POKE reaches kernel VAs — arbitrary kernel read/write
- **Location:** `sys/arch/i386/pmap.c:1460`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** arch/i386 memory
- **Defect:** pmap_copyin_other() (line 1397) and pmap_copyout_other() (line 1455) validate only that pmap->pdir_phys, the page-table phys, and the resolved data phys are all below the 0x40000000 direct-map ceiling. They never bound the caller-supplied user virtual address `uva` to user space (< KERN_BASE = 0xC0000000). Every user pmap has the kernel mapped in shared PDEs 768-1022 (copied in pmap_create), and kernel code/data lives in physical memory below 0x40000000, so a kernel VA passes all three bounds checks. copyout_other deliberately writes straight to the backing physical page, bypassing PTE write-protection. sys_ptrace() (sys/kern/ptrace.c:167,190) feeds the raw user-supplied `addr` into these helpers with no user/kernel split. PTRACE_ATTACH only requires matching euid (unprivileged users may trace their own processes, and PTRACE_TRACEME needs no check at all).
- **Failure scenario:** An unprivileged process forks a child that calls PTRACE_TRACEME (or attaches to its own child). The tracer issues PTRACE_POKEDATA with addr=0xC0000000+ (any kernel VA, e.g. its own struct proc's uid field or a syscall dispatch pointer). pmap_copyout_other walks the tracee pmap, finds the shared kernel PTE, computes pa < 0x40000000, and writes the attacker's word to (pa+0xC0000000) — arbitrary kernel memory write → root/kernel privilege escalation. PTRACE_PEEKDATA at a kernel VA symmetrically leaks arbitrary kernel memory. Fix: reject uva (and uva+len) >= KERN_BASE in both helpers, as sys_pmap_stats already distinguishes with KERN_BASE.
- **Verifier:** Read sys/arch/i386/pmap.c:1397-1500 and sys/kern/ptrace.c:87-195, plus pmap_create at 425-438. Every claim holds. (1) Both pmap_copyin_other (1397) and pmap_copyout_other (1455) bound ONLY physical addresses: pmap->pdir_phys<0x40000000 (1407/1460), pt_phys<0x40000000 (1426/1479), pa<0x40000000 (1435/1488). The caller-supplied uva is never compared to KERN_BASE. (2) pmap_create copies the kernel higher-half PDEs into every user pmap: lines 435-438 loop i=768..1022 copying kernel_pd[i] into pd[i]. So any VA>=0xC0000000 resolves through present, shared kernel PDEs in a tracee's pmap. (3) With the…

### PTY master close frees pair while slave still open (UAF)
- **Location:** `sys/drivers/console/pty.c:747`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** input/audio/virtio/devices
- **Defect:** pty_pair_t.slave_open is only ever initialized to 0 (memset in pty_pair_alloc_core) and re-zeroed in pty_slave_drv_close; it is NEVER incremented to 1. The Unix98 slave node's open callback is tty_fs_open (set in pty_publish_slave_node), which bumps slave_tty->count but not p->slave_open. Consequently pty_master_node_close() always reads slave_open==0 (line 727) and unconditionally calls pty_destroy(p) (line 747) whenever the master is closed, even while a process still holds the slave (/dev/pts/N) open. pty_destroy() kfree()s p, p->master_tty, and tty_free()s p->slave_tty, and unpublishes+frees the slave fs_node. The still-open slave fd's cloned fs_node->ptr keeps pointing at the freed slave_tty (tty_free does not zero the TTY_MAGIC field), so any subsequent slave read/write/ioctl/close dereferences freed memory. This is the exact telnetd/login topology the file's own comments describe (master in the forwarder, slave in the shell child).
- **Failure scenario:** Process A open(/dev/ptmx); TIOCSPTLCK unlock; Process B open(/dev/pts/N) and runs a shell on it; Process A close(master fd) (e.g. telnetd sees the socket drop). pty_master_node_close sees slave_open==0 -> pty_destroy frees slave_tty and the pair. Process B's next read()/write()/ioctl() on its slave fd runs tty_read/tty_write on the freed struct tty -> use-after-free (read of stale magic, then operates on / writes freed heap), kernel corruption or panic.
- **Verifier:** Every load-bearing claim is true in the actual code. (1) A grep of all of sys/ finds slave_open only in pty.c: declared (line 54), zeroed by memset in pty_pair_alloc_core (line 340), reset to 0 in pty_slave_drv_close (line 233), read (line 727) and tested (line 747). It is NEVER set to 1. (2) The Unix98 slave node's open is tty_fs_open (pty.c:299) → tty_open (tty.c:1430), which increments tty->count but pty_slave_driver (pty.c:240-247) has NO .open callback, so p->slave_open stays 0 when /dev/pts/N is opened. (3) pty_master_node_close (pty.c:717) reads slave_open under the lock and at line 747…

### kern_wait4 walks and reaps children with no locking — double-reap double-free
- **Location:** `sys/pm/wait.c:158`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** power/pm subsystem
- **Defect:** kern_wait4() takes no lock at all: find_waitable_child() walks parent->p_children/p_sibling and the zombie-reap sequence (proc_remove_child, pgrp_remove_proc, vm_map_destroy, sched_reap_process_threads, proc_destroy at lines 199-228) runs unlocked. Every other mutator of p_children (proc_add_child, proc_remove_child, proc_reparent_children in process.c) documents and takes proctree_lock, but the reader/reaper here does not. Nothing serializes two concurrent wait4() calls in the same process, nor wait4 against the P_AUTOREAP sweep, nor init's wait4 walk against proc_reparent_children head-inserting orphans into init->p_children mid-walk.
- **Failure scenario:** Two threads of the same multithreaded parent call wait4(-1,...) while a child is SZOMB (SMP, or UP with kernel preemption between find and reap). Both find the same target before either calls proc_remove_child; both then run vm_map_destroy(target->vm_map) and proc_destroy(target) → double vm_map_destroy + double kfree of the process_t → kernel heap corruption/panic. Same double-free if a racing wait4 finds a child in the window before proc_exit's P_AUTOREAP path removes it from the parent list — then both wait4 and proc_reap_autoreap_zombies() tear it down.
- **Verifier:** Read sys/pm/wait.c in full: kern_wait4() contains zero locking — find_waitable_child() (lines 70-139) walks parent->p_children/p_sibling unlocked, and the zombie-reap sequence (lines 199-228) sets no in-progress marker (target->state remains SZOMB throughout), so nothing stops a second concurrent finder. The apparent guard is illusory: proc_remove_child() (process.c:702-720) does take proctree_lock internally, but it is a void silent no-op when the child was already unlinked, so a losing racer proceeds anyway into vm_map_destroy(target->vm_map) (both threads can read it non-NULL before either …

### Single-thread wakeups swallowed by STOPPED/ZOMBIE head waiter
- **Location:** `sys/kern/sleepq.c:332`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** sync primitives
- **Defect:** All wake paths (sleepq_wake_one_internal, wake_n, wake_bitset, requeue) dequeue the head waiter unconditionally but only flip it to THREAD_READY if its state is not THREAD_STOPPED/THREAD_ZOMBIE. The wake is still counted and consumed. The file's own comment (lines 317-331) documents that threads killed during teardown can be marked THREAD_ZOMBIE while still linked in a sleepq. Every single-wake consumer — mutex_unlock (mutex.c:132), sema_post (semaphore.c:37), rw_runlock/rw_wunlock (rwlock.c:59/120), futex PI unlock (futex.c:1362) — therefore loses the wakeup entirely when the dequeued head is a zombie: no attempt is made to wake the next eligible waiter. posix_sem/ipc_sem defend against exactly this with a ~100ms sleep_expiry 'lost-wakeup net'; mutex/rwlock/semaphore/futex waiters have no such net (sleep_expiry stays 0).
- **Failure scenario:** Threads A and B of different processes block on kernel mutex M (A enqueued first). A's process is killed; A ends up THREAD_ZOMBIE while still linked at the queue head (the documented re-block-during-teardown window). The owner calls mutex_unlock: sleepq_wake_one dequeues zombie A, does not set it READY, returns non-NULL; B is never woken. M is now free with B parked forever — permanent hang of B (and anything waiting on B). Same with SIGSTOP: a stopped head waiter absorbs the only wake and other waiters stall until (if ever) SIGCONT.
- **Verifier:** Every element of the finding checks out against the actual code. (1) The swallow mechanism is real: sleepq_wake_one_internal (sys/kern/sleepq.c:309-342) unconditionally dequeues the head waiter, clears wait_chan, decrements sq_count, and only conditionally sets THREAD_READY ("if (t->state != THREAD_STOPPED && t->state != THREAD_ZOMBIE)", line 332), then returns t non-NULL regardless — the wake is consumed with no attempt to wake the next eligible waiter. Same pattern in wake_n (line 443), wake_bitset (line 504), and requeue (line 591). (2) The consumers behave exactly as claimed: mutex_unlock …


## HIGH

### send()/sendto()/sendmsg() memcpy raw user data buffer in kernel context (no copyin/bounce)
- **Location:** `sys/net/af_unix.c:1319`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** BSD sockets
- **Defect:** sys_send() passes the raw userspace data pointer straight to afunix_node_write(), which memcpy()s from it into the peer ring via afbuf_write() (lines 604, 641). The DGRAM helper afunix_dgram_deliver() (line 1459) and the AF_INET path afinet_sendto() (af_inet.c lines 985/1006, 458/482) do the same — they memcpy() the user buffer into a kernel packet buffer with no copyin(). The kernel page-fault handler (arch/i386/idt.c:386) only recovers kernel-mode faults when current_thread->on_fault is set, which is true only inside copyin/copyout — a plain memcpy from a bad user address is NOT fixed up and falls through to the fatal path. The recv side deliberately bounces through a kmalloc'd kernel buffer (do_recv/recv_into_kbuf, 'the recv() OOM panic') precisely to avoid this, but the send side has the symmetric hole. The sendmsg comment at line 1630 even asserts 'sys_send/sys_sendto copyin them on their own' — they do not.
- **Failure scenario:** An unprivileged process calls send(fd, p, len, 0) (or sendto/sendmsg) on an AF_UNIX or AF_INET socket where p (or a page within [p, p+len)) is unmapped/not-present. afunix_node_write()->afbuf_write() (or afinet_sendto's memcpy into pkt) executes memcpy(dst, p, n) in kernel mode with on_fault==0; the resulting page fault is unrecoverable -> kernel panic. Trivial local DoS.
- **Verifier:** The defect is real and trivially triggerable. sys_send (sys/net/af_unix.c:1287-1323) validates only the fd, then hands the raw userspace `buf` to afunix_node_write (line 1319), which memcpy's it into the peer ring via afbuf_write (memcpy at line 119, called from lines 604/641). No copyin/bounce. The same holds for the DGRAM helper afunix_dgram_deliver (afbuf_write at 1459) and afinet_sendto in af_inet.c (memcpy of raw buf into pkt at lines 985/1006 and 458/482). The perso dispatch tables (perso_native SYS_SEND, perso_linux:604) call sys_send directly with no wrapper copyin.

The page-fault han…

### sys_socketpair writes result fds directly to user sv[] pointer without copyout
- **Location:** `sys/net/af_unix.c:964`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** BSD sockets
- **Defect:** sys_socketpair() validates only 'if (!sv) return -EFAULT' and then, after both sockets are installed and peered, writes the file descriptors straight through the raw userspace pointer: 'sv[0] = fa; sv[1] = fb;' (line 964). Every other socket syscall in this file copies user sockaddr/optval buffers with copyin/copyout (NET-03), and the analogous sys_pipe() (kern/syscall.c:2946) uses copyout(kfds, fds, 2*sizeof(int)). A store through an unvalidated user pointer in kernel mode is not covered by the on_fault fixup path (arch/i386/idt.c:386), so a bad sv faults fatally; it is also an ABI/correctness defect (should go through copyout so a page-in or fault is handled).
- **Failure scenario:** Process calls socketpair(AF_UNIX, SOCK_STREAM, 0, sv) with sv pointing at an unmapped/read-only page. Both fds are created and installed, then 'sv[0]=fa' stores through the bad pointer in kernel mode with on_fault==0 -> unrecoverable kernel fault (panic/DoS); on a merely-not-yet-present writable page it also skips the demand-paging that copyout would trigger.
- **Verifier:** The code at sys/net/af_unix.c:927-965 confirms the finding. sys_socketpair validates only `if (!sv) return -EFAULT;` (line 929) and then, after both sockets are installed and peered, writes the fds straight through the raw user pointer: `sv[0] = fa; sv[1] = fb;` (line 964). No copyin/copyout, no validate_user_addr, no on_fault arming anywhere on that path. The four personality dispatchers (perso_linux.c:602, perso_freebsd.c, perso_netbsd.c:425, perso_openbsd.c:203, perso_native.c:79) all pass the raw user argument straight through as `(int*)a[3]`, so sv is fully user-controlled and the store i…

### OpenBSD getrusage writes to unvalidated user pointer
- **Location:** `sys/exec/perso/perso_openbsd.c:59`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** BSD/foreign personalities
- **Defect:** openbsd_sys_getrusage() receives the raw user-supplied `struct rusage *rusage` from the syscall dispatch and writes to it DIRECTLY — `memset(rusage, 0, sizeof(struct rusage))` followed by `rusage->ru_utime.tv_sec = ...` etc. — with no copyout and no validate_user_addr/access check. Every other stat/time/rusage wrapper in this subsystem (e.g. netbsd_sys_getrusage, netbsd_sys_getrusage50) fills a kernel-local struct and copyout()s it precisely to avoid this. Because user and kernel share one address space on i386, a caller-supplied pointer that is unmapped or points into kernel space is dereferenced and written by the kernel with no fault-handling contract.
- **Failure scenario:** An OpenBSD-personality process calls getrusage(RUSAGE_SELF, badptr) where badptr is unmapped or a kernel address: the kernel's memset/field stores fault in kernel context (panic) or, for a chosen valid kernel VA, corrupt arbitrary kernel memory — instead of the copyout path returning -EFAULT.
- **Verifier:** The defect is real and triggerable. openbsd_sys_getrusage() (sys/exec/perso/perso_openbsd.c:52-73) writes to the caller-supplied `struct rusage *rusage` DIRECTLY: `memset(rusage, 0, sizeof(struct rusage))` at line 59 followed by field stores at lines 64-70. There is no validate_user_addr(), no copyout(), and no on_fault arming. The handler is wired live into the OpenBSD dispatch table ([OPENBSD_SYS_getrusage] = &openbsd_sys_getrusage, line 194) and the `rusage` argument is the raw user value delivered by i386_extract_syscall_args (syscall.c:292); perso_id=PERS_OPENBSD is set from ELFOSABI_OPEN…

### FreeBSD gettimeofday overruns caller's 8-byte struct timeval
- **Location:** `sys/exec/perso/perso_freebsd.c:106`  ·  abi-correctness  ·  verdict **CONFIRMED**
- **Subsystem:** BSD/foreign personalities
- **Defect:** FREEBSD_SYS_gettimeofday is wired straight to native sys_gettimeofday, which copyout()s a native `struct timeval` (time_t + suseconds_t are both int64 => 16 bytes). FreeBSD i386 struct timeval is 8 bytes (32-bit time_t + 32-bit suseconds_t) — the repo's own `struct freebsd_timeval` in freebsd_user.h:69-72 declares it as int32_t+int32_t = 8 bytes, and compat.c:666-687 documents that FreeBSD i386 time_t is 32-bit (which is exactly why freebsd_sys_clock_gettime marshals to an 8-byte layout). The NetBSD side added netbsd_sys_gettimeofday for this very reason; FreeBSD has no equivalent wrapper, so the native handler writes 8 bytes past the caller's timeval.
- **Failure scenario:** A FreeBSD binary calls gettimeofday(&tv, NULL) with an 8-byte tv on the stack: copyout writes 16 bytes, clobbering the adjacent 8 bytes (saved register / stack canary / next local) — silent corruption or a stack-smash abort; if tv sits at a page boundary the extra 8 bytes fault and gettimeofday spuriously returns EFAULT.
- **Verifier:** perso_freebsd.c:106 wires FREEBSD_SYS_gettimeofday (116) directly to native sys_gettimeofday with no marshaling wrapper (grep confirms none exists, unlike netbsd_sys_gettimeofday at perso_netbsd.c:112). sys_gettimeofday (sys/kern/time.c:1035-1044) copyout()s sizeof(struct timeval) to the user pointer, and the kernel timeval is 16 bytes: sys/include/sys/types.h typedefs both time_t and suseconds_t as int64_t. The FreeBSD i386 ABI struct is 8 bytes, per the repo's own struct freebsd_timeval (freebsd_user.h:69-72, int32+int32), and compat.c:662-687's freebsd_sys_clock_gettime comment explicitly d…

### Double-free of segment pages on elf_load error paths
- **Location:** `sys/exec/formats/elf.c:756`  ·  resource-leak  ·  verdict **CONFIRMED**
- **Subsystem:** ELF loaders & exec
- **Defect:** In the legacy per-page PT_LOAD loader, each mapped physical page is linked into the segment's owning vm_object via vm_page_insert() (elf.c:783-789). Every error-cleanup path then frees those same pages TWICE: first with pmm_free_block(page_maps[pi].pa) in a manual loop, then again via vm_object_deallocate(seg_obj), which walks seg_obj->pages and calls vm_page_free()->vm_phys_free_page() on each. pmm_free_block() (arch/i386/pmm.c:1056) does NOT unlink the page from the object, so the page remains on seg_obj->pages after the first free. The three affected paths are: OOM after pmm_alloc_block (elf.c:748-759), pmap_enter failure (elf.c:765-775), and short file->read (elf.c:831-841). vm_page_free() clears PG_FREE (vm_page.c:606 'flags &= PG_PMM_ALLOC'), defeating the PG_FREE double-free guard in vm_phys_free_page (phys_mem.c:323), so the second free reaches the '!(page->flags & PG_PMM_ALLOC)' check (phys_mem.c:333) and panics with 'vm_phys: free of unallocated page'.
- **Failure scenario:** Craft an ELF whose PT_LOAD has p_filesz != p_memsz (a BSS tail forces the legacy path, bypassing the shared-file fast path) and p_filesz extending past end-of-file. During elf_load, all pages are allocated, mapped, and inserted into seg_obj; the file->read at elf.c:831 returns short, entering the read-fail cleanup which pmm_free_block()s every page then vm_object_deallocate(seg_obj)s them -> vm_phys_free_page sees a page already off the buddy allocator -> kernel panic (deterministic local DoS from an unprivileged execve). The OOM path is likewise reachable under memory pressure.
- **Verifier:** The double-free is real and the guard-defeat mechanism is exactly as described. I read all three sites.

LEGACY PATH ENTRY: A crafted ELF with p_filesz != p_memsz bypasses the shared-file fast path (elf.c:682 requires `phdr.p_filesz == phdr.p_memsz`) and enters the legacy per-page loader. seg_obj is allocated whenever current_process->vm_map exists (elf.c:735-737), which is always true during execve.

PAGE INSERTION: Each successfully mapped page is linked into seg_obj via vm_page_insert() (elf.c:783-789) AND recorded in page_maps (elf.c:792-793). So the same num_pages pages live in both struc…

### Failed execve after setuid does not roll back credentials or signal state
- **Location:** `sys/exec/formats/elf.c:1743`  ·  error-handling  ·  verdict **CONFIRMED**
- **Subsystem:** ELF loaders & exec
- **Defect:** elf_execve applies the setuid/setgid effective-credential change (current_process->euid = file->uid; suid/sgid updated, elf.c:1700-1710) and resets all signal dispositions to SIG_DFL (exec_reset_signals, elf.c:1692) BEFORE the last fallible step, exec_setup_stack (elf.c:1740). If exec_setup_stack fails (OOM in its kmalloc/pmm_alloc_block/pmap_enter, which returns -1), control jumps to the cleanup label. The cleanup path restores only perso_id and the old pmap/vm_map (elf.c:1863-1893); it never restores euid/egid/suid/sgid or the caller's original signal handlers. execve then returns an error to userland and the process resumes its ORIGINAL image with the target binary's elevated effective credentials and wiped signal handlers.
- **Failure scenario:** A process execs a setuid-root helper; memory pressure makes exec_setup_stack's page allocation fail after the euid=0 change has been committed. execve returns -1, but the calling process (still running attacker-controlled original code) now has euid=0 and default signal dispositions -> local privilege escalation and a POSIX signal-semantics violation.
- **Verifier:** The defect is real and matches the code. In elf_execve (sys/exec/formats/elf.c), inside the `if (current_process)` block: exec_reset_signals() (line 1692) wipes all caught signal dispositions to SIG_DFL, clears sig_catch, disables the alt-stack; then lines 1700-1710 commit the setuid/setgid change (current_process->euid = file->uid; egid; suid=euid; sgid=egid). These mutate current_process in place. The last fallible operation, exec_setup_stack (line 1740), runs AFTER these commits and genuinely returns -1 on allocation failure — kmalloc of stack_pages (line 1062), pmm_alloc_block (1076), pmap…

### uac_detach: use-after-free of freed usb_device via feeder kthread on hot-unplug
- **Location:** `sys/drivers/usb/uac.c:684`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** USB stack
- **Defect:** uac_detach() sets d->running=0 and wakes the feeder, but never joins/waits for the feeder kthread (uac_feeder), never clears d->udev, and never clears d->in_use. After uac_detach() returns, usb_disconnect_device() (usb.c:855) proceeds to usbdevfs_unpublish() and usb_free_device(), which kfree()s the usb_device_t. The feeder thread, if it is anywhere past its `while (d->running)` test at the moment of detach, goes on to call usb_frame_number(d->udev), usb_iso_schedule(d->udev, ...) and usb_iso_reclaim(d->udev, ...) — each of which dereferences d->udev (== the just-freed device) and d->udev->hcd. The driver's own comment (lines 696-698) admits 'Buffers are intentionally left allocated... Detach is not hit on the supported (non-hot-unplug) paths', but the root-port hot-plug monitor (usb.c usb_hotplug_scan) DOES call the bound driver's .detach on physical/qemu device_del, so this path is reachable. Additionally d->in_use stays 1 (slot permanently leaked) and d->fifo_buf/d->ring DMA memory are leaked on every unplug.
- **Failure scenario:** USB audio device (e.g. EarPods 05ac:110b or qemu -device usb-audio) is bound by uac and streaming; user issues qemu 'device_del' or physically unplugs. Hotplug monitor -> usb_disconnect_device -> uac_detach (running=0) -> usb_free_device frees the usb_device_t. The feeder kthread, mid-iteration, calls usb_iso_schedule(d->udev,...) -> dereferences freed dev->hcd -> use-after-free / kernel memory corruption. The uac slot and its DMA ring/FIFO are also permanently leaked.
- **Verifier:** uac_detach (sys/drivers/usb/uac.c:684-699) sets d->running=0 and wakes the feeder but has no join/wait, never clears d->udev or d->in_use; its own comment admits the feeder "may still be unwinding" and assumes detach is unreachable. That assumption is false: usb_hotplug_monitor is a live kthread (usb.c:949) scanning every ~250 ms, and on root-port disconnect calls usb_disconnect_device (usb.c:855-877), which runs ->detach then immediately kfree()s the usb_device_t via usb_free_device (usb.c:201). The feeder (uac.c:143-232) dereferences d->udev via usb_frame_number/usb_iso_schedule/usb_iso_recl…

### mountlist traversed lock-free while mount/unmount mutate and kfree() entries
- **Location:** `sys/vfs/vfs.c:1371`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** VFS & buffer cache
- **Defect:** The global `mountlist` TAILQ is read lock-free on nearly every path lookup: vfs_lookup() (line 701) and vfs_lookup_lstat() (line 781) do `TAILQ_FOREACH(mnt, &mountlist, ...)` once per resolved path component, dereferencing mnt->mnt_covered_ino / mnt_covered_mp / mnt_node_root; vfs_mount_root_parent() (line 95) and vfs_remount() (line 214) also walk it unlocked. Meanwhile vfs_mount_legacy() inserts (line 421) and vfs_unmount_legacy_flags() removes AND frees a mount (lines 1371-1372: `TAILQ_REMOVE(&mountlist,...)` then `kfree(target_mp, sizeof(struct mount))`) with NO serialization against those readers. The file's `vfs_mount_lock` spinlock only guards the per-node (FS_MOUNTPOINT flag, node->ptr) tuple (lines 72-75, 378-381, 1359-1362) — it is never held across mountlist membership changes or across the readers. On an SMP/preemptive kernel a lookup on one CPU can therefore follow a next-pointer into, or read fields out of, a struct mount that another CPU is concurrently TAILQ_REMOVE'ing and kfree'ing.
- **Failure scenario:** CPU A runs any path lookup (e.g. open('/mnt/foo/bar')) and enters the mount-crossing `TAILQ_FOREACH(mnt, &mountlist,...)` at vfs.c:701. Concurrently CPU B runs umount('/mnt') (or a hot-unplug via vfs_force_unmount_dev), which reaches vfs_unmount_legacy_flags() and executes TAILQ_REMOVE + kfree(target_mp) at 1371-1372 without any lock A observes. A dereferences the freed/poisoned mount node -> use-after-free read: garbage mnt_node_root substituted mid-traversal (wrong-filesystem crossing) or a kernel fault on a poisoned TAILQ next pointer.
- **Verifier:** Verified directly in /home/segin/substrate/sys/vfs/vfs.c. The header comment (lines 44-53) even claims vfs_mount_lock guards "plus mountlist", but the implementation never does: TAILQ_INSERT_TAIL at line 421 (vfs_mount_legacy) is unlocked, and in vfs_unmount_legacy_flags the spinlock is released at line 1362 BEFORE the TAILQ_REMOVE + kfree(target_mp, sizeof(struct mount)) at lines 1371-1372. Every reader is lock-free: vfs_lookup line 701 and vfs_lookup_lstat line 781 run TAILQ_FOREACH(mnt, &mountlist, ...) once per path component dereferencing mnt_covered_ino/mnt_covered_mp/mnt_node_root; vfs_…

### TLB shootdown ACK counter is global and shared across rounds; wait times out silently
- **Location:** `sys/arch/i386/pmap.c:1897`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** arch/i386 memory
- **Defect:** pmap_shootdown_wait() spins on the single global shootdown_ack_count until it reaches expected_cpus or a fixed 1,000,000-iteration timeout, then returns unconditionally with no error. shootdown_ack_count, shootdown_va/len/all are one shared request slot. If a round times out (a remote CPU is slow/in a long IRQ-off region), the initiator sets shootdown_pending=0 and releases shootdown_lock while a remote pmap_shootdown_handler IPI is still in flight. That late handler runs pmap_invalidate_page using the NEXT round's shootdown_va/len (already overwritten) and increments the NEXT round's shootdown_ack_count. The next initiator then sees an inflated ack count and returns before that CPU has actually invalidated the correct page.
- **Failure scenario:** On SMP: CPU0 shootdown of VA-A times out because CPU1 is IRQ-disabled >1e6 spins. CPU0 proceeds having freed/remapped VA-A's frame. CPU1's delayed IPI handler now flushes VA-B (the next request's address) and bumps ack_count. CPU0's next shootdown of VA-B returns immediately (ack already counted) though CPU1 never flushed VA-B — CPU1 keeps a stale writable TLB entry for a recycled physical frame → cross-address-space memory corruption / stale-mapping read.
- **Verifier:** Read sys/arch/i386/pmap.c:1751-1906, sys/include/sys/lock.h:40-50, sys/arch/i386/idt.c:585-591, sys/arch/i386/smp_discovery.c:676-678. All claimed mechanics are real: (1) pmap_shootdown_wait (pmap.c:1897-1906) spins on the global shootdown_ack_count with a fixed 1e6-pause timeout and returns silently with no error; callers (pmap_shootdown_page/_range/_all) unconditionally clear shootdown_pending and drop shootdown_lock. (2) shootdown_va/len/all/ack_count are a single shared slot (1752-1756); the IPI handler (1773-1785) reads them and __sync_fetch_and_add's ack_count without checking shootdown_…

### FAT root node not pinned in node cache — recycled after 64 lookups
- **Location:** `sys/fs/fat/fat.c:490`  ·  logic  ·  verdict **CONFIRMED**
- **Subsystem:** exFAT & FAT
- **Defect:** fat_alloc_node() hands out fs_node_t/fat_node_t from a fixed 64-slot round-robin cache: `int idx = fat_node_cache_idx++ % FAT_NODE_CACHE_SIZE;` with no special case for the root inode. fat_mount() allocates the mount's root node from this same round-robin (fat.c:840) and the pointer is retained long-term by the VFS (fs_root / mp->mnt_node_root / fs->root_node). Every finddir() also allocates from the ring. After FAT_NODE_CACHE_SIZE (64) node allocations the root's slot is memset and repopulated as some other file, so the still-referenced root fs_node silently becomes a different, often non-directory, inode. The sister exFAT driver documents and fixes exactly this bug by pinning the root to slot 0 (exfat.c:431-436, exfat_alloc_node); FAT was never given the same fix.
- **Failure scenario:** Mount a FAT volume, then perform 64+ path lookups/opens (e.g. `ls -R` or untar of a tree with >64 entries). The 64th allocation recycles the slot holding the root node; the retained root pointer now describes a regular file. Subsequent traversals of '/' dispatch file ops (read/write) instead of readdir/finddir, yielding ENOTDIR, empty directory listings, or a wrong-node-type callback on a shared cache slot.
- **Verifier:** fat_alloc_node (sys/fs/fat/fat.c:488-533) allocates from static 64-slot rings via `int idx = fat_node_cache_idx++ % FAT_NODE_CACHE_SIZE;` with no special case for FAT_ROOT_INO, and memsets both fat_node_cache[idx] and fat_fs_node_cache[idx] on reuse. fat_mount (fat.c:840) allocates the mount root from this same ring, and the VFS retains the raw pointer long-term: vfs.c:342-406 stores it into fs_root / mountpoint->ptr / mp->mnt_node_root, and traversal dereferences mnt_node_root directly (vfs.c:705, 785) — no copy is made. Every successful fat_finddir allocates a slot (fat.c:589 and 670), so 64…

### Cluster allocator is not atomic — concurrent allocation cross-links files
- **Location:** `sys/fs/fat/fat.c:946`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** exFAT & FAT
- **Defect:** Neither driver takes any lock (grep confirms zero mutex/spinlock/lockmgr use in fat.c and exfat.c). fat_alloc_cluster() scans the FAT/cache for an entry==0, then marks it EOC in a separate, non-atomic read-then-write; exfat_alloc_cluster() (exfat.c:180) does the same over fs->bitmap plus the un-serialized fs->free_clusters counter. Two threads (SMP or preemptive) that allocate concurrently both observe the same free cluster and both claim it, cross-linking two files onto one cluster and corrupting fs->free_clusters. The same unlocked mutation of the shared in-memory bitmap corrupts the bitmap byte via lost read-modify-write updates.
- **Failure scenario:** Two processes each write to (or create) a different file on the same FAT/exFAT mount at the same time. Both call *_alloc_cluster, both scan and find cluster C free, both write EOC to C and link C into their own chain. The two files now share cluster C; one file's data overwrites the other's, and the free-cluster accounting (statfs f_bfree) is wrong.
- **Verifier:** Verified by reading the actual code. (1) Zero locking in either driver: grep for mutex_lock/spinlock/lockmgr in sys/fs/fat/fat.c and sys/fs/exfat/exfat.c returns 0 hits in both files. (2) No serialization at any higher layer on this path: sys/kern/syscall.c kern_write() (line ~407) fetches f->f_data and calls write_fs() directly with no lock; sys/vfs/vfs.c write_fs() (line 442) is just `if (node->write) node->write(...)` — no vnode or mount lock is taken. The lockmgr vnode locks mentioned in the architecture docs are not on this fs_node_t write path. There is no big kernel lock, and the kernel…

### FAT write/directory paths share function-static scratch buffers with no locking
- **Location:** `sys/fs/fat/fat.c:1086`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** exFAT & FAT
- **Defect:** The FAT write path uses process-lifetime `static` scratch buffers shared by all callers with no serialization: fat_file_write() `static uint8_t cluster_buf[32768]` (1086), fat_find_dir_space() `static uint8_t dir_buf[32768]` (1258), fat_dir_add_entry() `static fat_lfn_t lfn_entries[20]` (1356) and `static uint8_t sector_buf[4096]` (1365), fat_dir_remove_entry() `static uint8_t dir_buf[32768]` (1401), fat_create_entry() `static uint8_t zero_buf[32768]` (1503). On a preemptive/SMP kernel two threads in these routines clobber each other's buffer between the read-modify and the write-back. (The read path and the whole exFAT driver correctly use per-call kmalloc; only these FAT write helpers use statics.)
- **Failure scenario:** Thread A in fat_file_write memcpy's its data into the shared static cluster_buf and is preempted before fat_write_sectors; Thread B (writing a different file) overwrites cluster_buf with its own data; Thread A resumes and writes B's bytes into A's cluster. Result: silent file data corruption. The same race in fat_dir_add_entry/fat_dir_remove_entry corrupts directory entries.
- **Verifier:** All six statics exist exactly where claimed in /home/segin/substrate/sys/fs/fat/fat.c: `static uint8_t cluster_buf[32768]` at line 1086 inside fat_file_write, `static uint8_t dir_buf[32768]` at 1258 (fat_find_dir_space), `static fat_lfn_t lfn_entries[20]` at 1356 and `static uint8_t sector_buf[4096]` at 1365 (fat_dir_add_entry), `static uint8_t dir_buf[32768]` at 1401 (fat_dir_remove_entry), `static uint8_t zero_buf[32768]` at 1503 (fat_create_entry). A grep for mutex/lock/spin in fat.c finds nothing but the `f_blocks` statfs field — the file has zero synchronization. The layers above provide …

### FAT directory-entry write assumes physical contiguity across a cluster boundary
- **Location:** `sys/fs/fat/fat.c:1367`  ·  logic  ·  verdict **CONFIRMED**
- **Subsystem:** exFAT & FAT
- **Defect:** fat_find_dir_space() lets a free-slot run span a cluster boundary: `consecutive` and start_sector/start_off/start_cluster are only reset when a USED entry is hit, and the scan (including the just-extended new cluster, set via next=nc at 1328) keeps counting across clusters. It returns the run's start_sector (in the first cluster). fat_dir_add_entry() then writes the total_entries sequentially with a plain `sector++` (1388-1392), which assumes the free run occupies physically contiguous sectors. When the run crosses into a non-contiguous next cluster (the common case for a freshly fat_alloc_cluster'd extension cluster, or any fragmented directory), sector++ walks off the end of the first cluster into whatever sectors physically follow it on disk rather than into the real next cluster.
- **Failure scenario:** A directory whose last existing cluster has fewer trailing free slots than needed (e.g. 1 free slot, but an LFN name needs 3 entries). fat_find_dir_space extends the directory (nc, non-contiguous), counts 1 slot in the old cluster + 2 in nc, returns start in the old cluster. fat_dir_add_entry writes entry 0 into the old cluster's last slot, then sector++ writes entries 1 and 2 into the on-disk sectors immediately following the old cluster (unrelated file data or metadata) instead of into nc, corrupting adjacent on-disk data and leaving the created entry unreadable.
- **Verifier:** Read sys/fs/fat/fat.c directly. (1) In fat_find_dir_space (lines 1256-1333), `consecutive`/`start_sector`/`start_off`/`start_cluster` are function-scope and only reset on a used entry (line 1313); the cluster-chain loop advances `cluster = next` (1330) — including into a freshly allocated, zeroed extension cluster via `next = nc` (1320-1328) — without resetting the run, so a free run genuinely straddles cluster boundaries and the function returns start_sector in the first cluster. (2) fat_dir_add_entry (1360-1393) writes total_entries with plain `byte_off += 32; if (byte_off >= bytes_per_secto…

### Unbounded eh_ecount in ext4 extent resolver → OOB read
- **Location:** `sys/fs/ext2/ext2.c:680`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** ext2 filesystem
- **Defect:** ext4_extent_resolve() reads eh_ecount (uint16, on-disk/attacker-controlled) into n and iterates ex[i] / idx[i] for i in [0,n) without ever validating n against eh_max or against the actual capacity of the container. The inline header lives in inode->i_block[] (only 60 bytes = 4 extents of 12 bytes after the 12-byte header); a deeper node is read into `scratch`, a single fs->block_size buffer. eh_max is read elsewhere but never used as a bound here. A crafted extent header with eh_magic=0xF30A and eh_ecount=0xFFFF makes the leaf loop read ex[0..65534] = up to ~786 KiB past the inode struct (depth 0) or past the block_size scratch buffer (depth>0).
- **Failure scenario:** Mount an ext4 image (INCOMPAT_EXTENTS) containing a file whose inode has EXT4_EXTENTS_FL, eh_magic=0xF30A, eh_depth=0, eh_ecount=0xFFFF. Any read of that file calls ext2_get_block_num→ext4_extent_resolve, which loops ex[i] for 65535 iterations reading far past the 60-byte i_block region (or past the block-sized scratch buffer for depth>0), yielding an out-of-bounds kernel read / crash / info-leak. The write path ext4_extent_alloc_inode_block has the mirror problem: `ne=&exts[n]` with n up to a bogus eh_max writes OOB into/after the inode struct.
- **Verifier:** I read sys/fs/ext2/ext2.c (ext4_extent_resolve at 648-698, ext4_extent_alloc_inode_block at 910-985, ext2_get_block_num at 700-708) and the struct/constant defs in ext2.h (113-148, 224).

The defect is real. In ext4_extent_resolve the only validation of the on-disk extent header is `if (eh->eh_magic != EXT4_EXT_MAGIC) return 0;` (line 652/674). Both eh_ecount and eh_max are uint16_t fields read straight off disk (ext2.h:124-125), fully attacker-controlled for any mounted image.

Leaf loop (679-694): `ext4_extent_t *ex = (ext4_extent_t *)(node + sizeof(*eh)); int n = eh->eh_ecount;` then `for (…

### blocks_per_group / inodes_per_group not bounded to block_size*8 → OOB bitmap scan
- **Location:** `sys/fs/ext2/ext2.c:2213`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** ext2 filesystem
- **Defect:** At mount, s_blocks_per_group is only checked !=0 and s_inodes_per_group is only checked !=0 and <= s_inodes_count. Neither is validated against block_size*8, the number of bits a single bitmap block can hold. The block and inode bitmaps are read into one-block buffers (fs->active_bg_bitmap, fs->active_inode_bg_bitmap, each fs->block_size bytes), then scanned with bits_in_group = blocks_per_group (ext2_alloc_block, line 2491, via ext2_find_next_zero_bit which clamps only to total_bits) and bits_in_group = inodes_per_group (ext2_alloc_inode loop, line 2604). A value larger than block_size*8 makes the scan index the single-block buffer far past its end.
- **Failure scenario:** Mount a crafted ext2 superblock with block_size=1024 (bitmap holds 8192 bits) and s_blocks_per_group=0xFFFFFFFF (or s_inodes_per_group=100000 with a large s_inodes_count). The first allocation reads one 1024-byte bitmap block, then ext2_find_next_zero_bit / the alloc_inode loop scan up to blocks_per_group/inodes_per_group bits, reading bitmap_buf[i/8] for i far beyond 8192 → heap out-of-bounds read, possible false 'free' bit found and a bogus block/inode handed out.
- **Verifier:** Verified in sys/fs/ext2/ext2.c. Mount validation (lines 2211-2228) checks only blocks_per_group != 0 and inodes_per_group in (0, s_inodes_count]; neither is bounded to block_size*8. s_inodes_count is itself attacker-controlled, so the inodes_per_group upper check does not bound it to the bitmap's bit capacity. The bitmap buffers are single blocks: active_bg_bitmap = kmalloc(fs->block_size) (line 2383) and active_inode_bg_bitmap = kmalloc(fs->block_size) (line 2393), each holding block_size*8 bits. ext2_alloc_block (line 2491) sets bits_in_group = blocks_per_group and passes it to ext2_find_nex…

### shmfs_write: size_t overflow of (off+sz) defeats grow → OOB write
- **Location:** `sys/fs/shmfs.c:196`  ·  integer-overflow  ·  verdict **CONFIRMED**
- **Subsystem:** minix/udf/9p/sysv/procfs
- **Defect:** shmfs_write computes `size_t end = (size_t)off + sz` then shmfs_grow(inode, end) and memcpy(inode->data + off, buf, sz). off is a caller-controlled off_t; the only guard is off<0. On i386 size_t is 32-bit: a large off truncates/wraps so `end` becomes small, shmfs_grow is a no-op (want<=data_cap), but the memcpy destination inode->data+off still uses the real 32-bit offset which is far beyond the tiny backing buffer.
- **Failure scenario:** On a /dev/shm object: lseek(fd, 0xFFFFF000, SEEK_SET); write(fd, buf, 0x2000). end = 0xFFFFF000+0x2000 wraps to 0x1000, grow allocates one page, then memcpy writes 0x2000 bytes at data+0xFFFFF000 → ~4 GiB out-of-bounds kernel write → corruption/fault.
- **Verifier:** The defect is real and unprivileged-reachable. In sys/fs/shmfs.c:193-202, shmfs_write's only guard is `off < 0`. off_t is 64-bit (sys/include/sys/types.h:46: typedef int64_t off_t) while i386 size_t is 32-bit, so `size_t end = (size_t)off + sz` truncates and wraps: for off=0xFFFFF000, sz=4096, end computes to 0. shmfs_grow (line 160: `if (want <= inode->data_cap) return 0`) is then a no-op, but the subsequent `memcpy(inode->data + off, buf, sz)` uses the full untruncated off (0xFFFFF000), producing a ~4 GiB out-of-bounds kernel write. The dangerous path is fully reachable with no upstream clam…

### pgrp_remove_proc reads and frees old_pgrp after dropping proctree_lock
- **Location:** `sys/pm/pgrp.c:213`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** power/pm subsystem
- **Defect:** pgrp_remove_proc() unlocks proctree_lock at line 208 and only then evaluates `if (free_sess || old_pgrp->pg_members == NULL)` and either kfree()s the pgrp or calls pgrp_signal(old_pgrp,...) which walks pg_members unlocked (pgrp_signal itself, line 410, never takes proctree_lock). The pg_members re-read at line 213 is a TOCTOU on state another remover can change and free after the unlock.
- **Failure scenario:** Processes P1 and P2 are the last two members of group G and exit concurrently (each exit calls pgrp_remove_proc via proc_release_zombie_resources / kern_wait4). Remover A removes P1 under the lock, sees pg_members={P2} (non-empty), unlocks. Remover B removes P2, sees the group empty, unlinks G and kfree()s it. A then dereferences freed G at line 213: if the freed word reads NULL, A kfree()s G a second time (double free); otherwise A walks G's freed member list in pgrp_signal → use-after-free, heap corruption.
- **Verifier:** The code is exactly as the auditor described. In /home/segin/substrate/sys/pm/pgrp.c, pgrp_remove_proc() (lines 191-224) does: lock proctree_lock (200), __pgrp_remove_proc(proc) (201), and if the group is still non-empty it only computes orphaned/has_stopped and leaves free_sess=NULL (202-204); if empty it unlinks the group via __pgrp_unlink_locked (206). It then unlocks at line 208 and ONLY AFTERWARD evaluates `if (free_sess || old_pgrp->pg_members == NULL)` at line 213, kfree()ing old_pgrp at 214 or calling pgrp_signal(old_pgrp,...) at 220-221. pgrp_signal (lines 410-420) walks pg_members wi…

### proc_exit publishes reapable state while threads may still run on other CPUs
- **Location:** `sys/pm/process.c:1792`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** power/pm subsystem
- **Defect:** proc_exit() marks every thread of the process THREAD_ZOMBIE (including threads currently RUNNING on remote CPUs — the code at line 1727 admits 'an IPI would force them off remote CPUs. We assume they are effectively dead here', but no IPI is sent and nothing waits for them to go off-CPU) and sets SZOMB, then wakes the parent. wait_threads_all_zombie()/find_waitable_child gate reaping only on state==THREAD_ZOMBIE, which is satisfied instantly, so the parent's wait4 can call sched_reap_process_threads() → sched_release_thread_storage() and free kernel stacks still in active use. The preempt_disable() at line 1769 protects only the local CPU; on SMP the parent runs concurrently on another core.
- **Failure scenario:** SMP: thread A of a multithreaded process calls exit() on CPU0 while sibling thread B executes a syscall on CPU1; parent blocks in wait4 on CPU1's runqueue. proc_exit flips B to THREAD_ZOMBIE and SZOMB; the sleepq_wake_all/sched_wakeup at lines 1795/1832 makes the parent runnable before the exiting thread's final sched_yield; parent reaps: B's kstack (and the exiting thread's own kstack) are pmm_free'd while CPU1/CPU0 still execute on them → arbitrary kernel-stack corruption, garbage-EIP fault.
- **Verifier:** All load-bearing claims verified against the source. sys/pm/process.c: proc_exit sets every thread of the process to THREAD_ZOMBIE with plain stores (lines 1677-1681, 1718-1725, and 1792-1796 — the last loop includes the current thread), sets SZOMB (1773), then wakes the parent (sched_wakeup at 1832) before its own sched_yield (1842). The comment at 1727-1729 explicitly admits "an IPI would force them off remote CPUs. We assume they are effectively dead here" — and grep confirms no IPI is sent and nothing waits for remote threads to go off-CPU. The preempt_disable at 1769 (comment 1754-1768) r…

### Concurrent wait4() double-frees a zombie child
- **Location:** `sys/pm/wait.c:160`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** process & signals
- **Defect:** kern_wait4() performs find_waitable_child() and then the entire zombie teardown (proc_remove_child, pgrp_remove_proc, vm_map_destroy/pmap_release, ldt_free_process, sched_reap_process_threads, procfs_release_pid_nodes, proc_destroy) with NO lock held and NO per-target 'claimed' flag set on the zombie before teardown begins. grep confirms wait.c takes no mutex/spinlock/preempt_disable anywhere. find_waitable_child() returns a bare pointer to a target still linked on parent->p_children. proc_remove_child/proc_destroy take internal locks individually, but the find->teardown sequence is not atomic, so two callers that both captured the same target before either unlinked it will both run the destructive teardown. proc_destroy() (spinlock + proc_storage_free/kfree) and vm_map_destroy() then run twice on the same object.
- **Failure scenario:** A multithreaded process (POSIX permits concurrent waitpid(-1) from two threads) has one zombie child. Thread A calls kern_wait4, find_waitable_child returns the zombie (reason 0). Before A reaches proc_remove_child, a timer preempt (kernel preemption is enabled; no preempt_disable here) or a second CPU lets thread B run kern_wait4; B's find_waitable_child returns the same zombie (still on the list). Both threads execute the case-0 teardown: two vm_map_destroy() and two proc_destroy()/proc_storage_free() calls on one process_t/vm_map -> kernel-heap double-free and page-directory double-free, corrupting the allocator (the same class of 'free of unallocated page' the codebase fought elsewhere).
- **Verifier:** Read /home/segin/substrate/sys/pm/wait.c in full: kern_wait4() (line 150) calls find_waitable_child() (line 158) and, for reason 0 (zombie), runs the entire teardown — proc_remove_child (199), pgrp_remove_proc (202), vm_map_destroy/pmap_release (204-209), ldt_free_process (211), sched_reap_process_threads (214), procfs_release_pid_nodes (219), proc_destroy (228) — with no lock, no preempt_disable, and no claim flag on the target. A grep of wait.c for mutex/spinlock/lock hits only comments; the P_WAITED flag is set only in the stopped path (case 1, line 239), never in the zombie path. find_wait…

### exec raises setuid/setgid credentials and resets signal dispositions before the last fallible step
- **Location:** `sys/exec/formats/elf.c:1700`  ·  logic  ·  verdict **CONFIRMED**
- **Subsystem:** process & signals
- **Defect:** elf_execve() applies the POSIX setuid/setgid credential change (current_process->euid = file->uid; egid/suid/sgid updated) and calls exec_reset_signals() at lines ~1690-1727, BEFORE exec_setup_stack() (line 1740), which is fallible and jumps to the 'cleanup' label on failure. The cleanup label (lines 1862-1900) restores perso_id, pmap, and vm_map, but never restores the credentials or signal dispositions. Because the credential change is not deferred to the point of no return, a post-credential exec failure returns -errno to userspace while the process resumes executing the ORIGINAL image with the elevated euid/egid and default-reset signal handlers.
- **Failure scenario:** An unprivileged process execs a setuid-root binary. elf_load() of the main image and interpreter succeed, so euid is set to 0 (file->uid) and signal handlers are reset. exec_setup_stack() then fails (e.g. ENOMEM while committing user-stack pages under memory pressure). Control goes to cleanup, the old address space is reinstated, and execve returns -1 -- but current_process->euid is now 0. The old program keeps running with root effective UID: a local privilege-escalation primitive whenever an attacker can force exec_setup_stack to fail on a setuid image. The reset signal dispositions are likewise left applied to the surviving old image.
- **Verifier:** Verified directly in sys/exec/formats/elf.c. exec_reset_signals() (line 1692) and the setuid/setgid credential change (euid/egid/suid/sgid at lines 1700-1710) execute inside the `if (current_process)` block BEFORE exec_setup_stack() (line 1740). exec_setup_stack is genuinely fallible: it returns -1 on kmalloc failure (line 1060), pmm_alloc_block failure (line 1069), or pmap_enter failure (line 1079), and elf_execve does `goto cleanup` on that (line 1743). At that point vm_state_committed is still 0 (it only flips at line 1771, after a successful stack setup), so the cleanup label restores old_…

### GPT entry array parsed with unbounded on-disk entry_size -> OOB read
- **Location:** `sys/kern/geom/geom_gpt.c:148`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** storage drivers
- **Defect:** geom_gpt_sniff() validates hdr->entry_size only for a lower bound (line 124: entry_size < 128 rejected) and never an upper bound. entry_buf is a fixed 16 KiB static array (512*32) and only up to 32 sectors (16 KiB) are ever read into it (entry_sectors capped at 32, line 138). However crc32(entry_buf, max_entries * hdr->entry_size) at line 148, and the per-entry pointer arithmetic entry_buf + i * hdr->entry_size at lines 162 and 189, use max_entries (up to 128) times the attacker-controlled entry_size with no clamp to the buffer size. Any entry_size > 128 combined with a large enough num_entries makes these reads run far past the 16 KiB buffer. The GPT header/entries come from attacker-influenced on-disk data and this runs at partition-scan time for every registered disk including removable USB mass storage and mounted disk images.
- **Failure scenario:** A crafted/removable GPT disk sets header entry_size=256 and num_entries=128 (max_entries clamped to 128). crc32() then reads 128*256 = 32768 bytes from the 16384-byte entry_buf (16 KiB OOB read), and the entry loop dereferences geom_gpt_entry structs at offsets up to 127*256=32512, all past the buffer. This leaks/registers partition geometry from adjacent kernel memory and can #PF/panic if the trailing pages are unmapped.
- **Verifier:** The code at sys/kern/geom/geom_gpt.c matches the claim. hdr->entry_size is a uint32_t read straight from the on-disk GPT header (geom.h:254). Line 124 validates only a lower bound (entry_size < 128 rejected); there is no upper bound anywhere. entry_buf is a fixed static 16384-byte array (line 137, 512*32) and at most 32 sectors / 16384 bytes are read in (lines 134/138/140). But line 148 computes crc32(entry_buf, max_entries * hdr->entry_size) where max_entries is clamped to 128 (line 130) and entry_size is NOT clamped: entry_size=256,num_entries=128 gives length 32768 -> a 16 KiB out-of-bounds…

### GPT entries_per_sector division by zero when entry_size > 512
- **Location:** `sys/kern/geom/geom_gpt.c:134`  ·  error-handling  ·  verdict **CONFIRMED**
- **Subsystem:** storage drivers
- **Defect:** entries_per_sector = 512 / hdr->entry_size (line 133) evaluates to 0 whenever the on-disk hdr->entry_size exceeds 512, because entry_size is only lower-bounded (>=128) and has no upper bound. Line 134 then computes entry_sectors = (max_entries + entries_per_sector - 1) / entries_per_sector, dividing by zero. On x86 an integer divide by zero raises #DE, which in the kernel partition-scan path faults/panics the kernel. Triggered by attacker-controlled on-disk GPT data during disk registration (e.g. inserting a USB stick or mounting a crafted image).
- **Failure scenario:** A GPT header with a valid signature/CRC and entry_size = 0x1000 (4096, > 512) makes entries_per_sector = 512/4096 = 0; the division at line 134 divides by zero and raises a #DE, crashing the kernel while scanning the disk for partitions.
- **Verifier:** Read sys/kern/geom/geom_gpt.c. Line 124 validates entry_size with a lower bound only (`hdr->entry_size < 128`) and no upper bound. entry_size is a raw uint32_t from the on-disk header (struct geom_gpt_header, geom.h:254). Line 133 computes `entries_per_sector = 512 / hdr->entry_size`; for any entry_size > 512 this unsigned division yields 0, and line 134 `(max_entries + entries_per_sector - 1) / entries_per_sector` divides by zero → #DE, a kernel fault/panic on i386. No guard exists between line 124 and line 134; the `entry_sectors > 32` clamp at line 138 is after the faulting division. All ga…

### proc_exit force-releases mutexes of siblings still running on other CPUs
- **Location:** `sys/kern/mutex.c:146`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** sync primitives
- **Defect:** mutex_release_owned_by_thread(thread) is called from proc_exit's FOREACH_THREAD loop (sys/pm/process.c:1667) for every sibling thread. Marking a sibling THREAD_ZOMBIE does not stop it if it is currently executing on another CPU (no IPI/handshake), yet this function walks and unlinks owner->held_mutexes and force-unlocks each mutex (m->owner = NULL; __sync_lock_release(&m->locked)) while the victim may be (a) concurrently mutating its own held_mutexes list inside mutex_lock/mutex_unlock (mutex_track_owner/mutex_untrack_owner run with no common lock -> torn list traversal, wild pointer deref) and (b) still executing inside the critical section the mutex protects — a third thread can then acquire the 'released' mutex and enter the same critical section concurrently, corrupting the protected data.
- **Failure scenario:** SMP: thread B (CPU1) holds kernel mutex M inside a critical section and is about to mutex_lock a second mutex (updating held_mutexes). Thread A (CPU0) of the same process calls exit_group -> proc_exit -> mutex_release_owned_by_thread(B) walks B's held_mutexes concurrently with B's update (use of a half-linked node) and force-unlocks M while B is mid-critical-section; waiter C acquires M and both B and C mutate the protected structure -> kernel data corruption / crash.
- **Verifier:** Read sys/kern/mutex.c and sys/pm/process.c directly. (1) The dangerous path is exactly as described: proc_exit (sys/pm/process.c:1661-1681) calls mutex_release_owned_by_thread(thread) at line 1667 for every sibling BEFORE setting THREAD_ZOMBIE at 1679, and nothing earlier in proc_exit (lines 1528-1660 are IPC/VT cleanup and instrumentation) stops or IPIs siblings; the code's own comment at lines 1727-1729 concedes "an IPI would force them off remote CPUs. We assume they are effectively dead here." — no IPI is ever sent (grep confirms that comment is the only IPI reference in process.c). A sibl…

### Runtime FBIOPUT_VIDEO_MODE desyncs fb geometry from console shadow buffer -> OOB shadow read
- **Location:** `sys/drivers/video/fb.c:593`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** video & console
- **Defect:** The /dev/fb0 ioctl FBIOPUT_VIDEO_MODE (fb_fs_ioctl, fb.c:584-597) calls current_driver->set_mode(mode_id) with a user-supplied mode id. Both drivers mutate the global fb geometry directly: bga_set_mode sets fb.width/fb.height (bga.c:380-381) and vga_set_mode_internal sets fb.width/fb.height (vga.c:483-484). However the framebuffer-console shadow buffer (fbcon_shadow, size fbcon_shadow_sz = boot fb.width*fb.height, and fbcon_shadow_w/h) is allocated ONLY by fbcon_shadow_setup(), which is invoked exclusively from fb_console_init() at boot (fb_console.c:2202). Nothing re-runs fbcon_shadow_setup() after a runtime mode change. fbcon_present() (fb_console.c:221-261) clips the pushed rectangle to the NEW fb.width/fb.height, then indexes the STALE shadow via `fbcon_shadow + row*fbcon_shadow_w + px`. If the new mode is larger than the boot mode, row can reach new_fb.height-1 and px new_fb.width-1, so the linear index exceeds fbcon_shadow_sz (old_w*old_h), reading adjacent kernel heap and painting those bytes to VRAM (info disclosure onto screen) and potentially faulting when the read crosses an unmapped page.
- **Failure scenario:** System boots at 640x480 (shadow allocated 640*480 bytes). A userland process opens /dev/fb0 and issues FBIOPUT_VIDEO_MODE selecting a 1920x1080 BGA mode. bga_set_mode updates fb.width=1920/fb.height=1080 but the shadow stays 640x480. On the next console repaint/scroll the accumulated dirty rect (clamped to 1920x1080) is flushed via fbcon_present, which reads fbcon_shadow[row*640 + px] for row up to 1079 -> index up to ~690000 into a 307200-byte allocation: OOB kernel-heap read, garbage rendered to the screen, and a possible page fault/panic.
- **Verifier:** The bug is real. fb.c:584-596 (FBIOPUT_VIDEO_MODE) copies in a user mode_id and calls current_driver->set_mode() with no console notification. bga_set_mode (bga.c:380-381) sets fb.width/fb.height to the new geometry. The console shadow is allocated only in fbcon_shadow_setup() (fb_console.c:266-290, size = fb.width*fb.height at alloc time, recorded in fbcon_shadow_w/h/sz), which is called exclusively from fb_console_init() (fb_console.c:2202), whose only callers are boot-time video_init paths (fb.c:1078, 1097, 1270). Grep confirms no re-run after a runtime mode change, and fbcon_use_shadow/poi…

### UMA page hash walked without slab lock — use-after-free of freed slab headers
- **Location:** `sys/vm/uma_core.c:998`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** vm allocators
- **Defect:** uma_page_hash is mutated (uma_hash_insert/uma_hash_remove, then pmm_free_block of the slab page) only under uma_slab_lock, but it is read with NO lock in three places: uma_zfree()'s double-free-marker lookup (line 998 -> uma_item_marker -> uma_find_slab), uma_zalloc()'s fast-path marker write (line 909, protected only by local intr_disable which gives no cross-CPU exclusion), and uma_item_size() (line 705, called from krealloc with no protection at all). For on-page slabs the uma_slab_t header lives inside the slab page itself, so when another CPU frees the last item of a slab (uma_zfree_slab_locked -> uma_slab_free -> pmm_free_block) the header memory returns to the buddy allocator and can be reallocated/scribbled immediately. An unlocked walker that already loaded a pointer to that slab then dereferences us_hnext/us_data/us_zone from recycled memory, or writes the ALLOCED marker byte (line 910) into a page now owned by someone else.
- **Failure scenario:** SMP, kmalloc/kfree load on the same size class from two CPUs: CPU A calls kfree(p,64) and is walking the hash chain for p's page in uma_find_slab; CPU B simultaneously frees the last item of another kmem-64 slab hashed to the same bucket, freeing its page to the buddy allocator, which hands it to a third allocation that overwrites the on-page header. CPU A loads us_hnext from the scribbled header -> wild pointer dereference or a stray marker-byte write into the new owner's data -> kernel heap corruption or panic.
- **Verifier:** All three unlocked readers exist as claimed in sys/vm/uma_core.c: (1) uma_zfree line 998 calls uma_item_marker->uma_find_slab with no lock and IRQs enabled (the intr_disable is later, line 1022) and then WRITES *df_mark=UMA_ITEM_BUCKETED (line 1005) into the slab page; kfree drops kmem_stats_lock before uma_zfree so nothing wraps it. (2) uma_zalloc line 909 does the same lookup plus a marker write (line 910) under only intr_disable(), which the file's own lock comment (lines 58-61) admits "gives no cross-CPU exclusion". (3) uma_item_size walks uma_page_hash at line 705 with zero protection, ca…

### In-place COW eviction frees a still-mapped frame; later pmap_enter unholds a recycled frame
- **Location:** `sys/vm/vm_object.c:172`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** vm allocators
- **Defect:** vm_object_add_page()'s stale-page eviction (lines 171-175) calls vm_page_free() on the old resident page with NO pv_list/wire_count/ref_count guards — in sharp contrast to the equivalent VM-09 reclaim in vm_fault.c lines 368-376, which explicitly requires pv_list==NULL, wire_count==0, ref_count<=1 'so a live PTE can never be left pointing at a freed frame'. The in-place COW path hits this: vm_fault step 5 (vm_fault.c:340) triggers when obj==first_obj && ref_count>1 (real after vm_map clip/split takes extra object references, e.g. mprotect splitting an anon MAP_PRIVATE entry — vm_map.c lines 887/954/1096). vm_object_add_page(first_obj,new_m) at vm_fault.c:358 then evicts and frees cow_src while the faulting process's read-only PTE still maps it (vm_page_free force-zeroes ref_count at vm_page.c:600, bypassing all hold accounting, and pv_remove_all destroys the pv entries without removing PTEs). Only afterwards does pmap_enter (vm_fault.c:393) replace the PTE — and pmap.c:898-899 does pv_remove + vm_page_unhold on the old (already buddy-freed, possibly re-allocated) frame.
- **Failure scenario:** Process mmaps anon MAP_PRIVATE, read-faults page P (RO mapping installed since ref>1), mprotect splits the region (object ref_count becomes 2), then writes P: COW allocates new_m, add_page frees P's frame while the PTE still points at it. Another CPU allocates that frame (ref_count reset to 1 by vm_phys_prepare_allocated_block); pmap_enter then vm_page_unhold()s it, dropping the NEW owner's hold to 0 -> the frame is freed under the new owner later -> double-allocation / silent kernel heap corruption. A second thread of the process can also read the freed/reused frame through the stale RO PTE in the window (cross-allocation data leak).
- **Verifier:** The defect is real and reachable. vm_object_add_page() (sys/vm/vm_object.c:171-175) evicts a stale resident page with an unconditional vm_object_remove_page()+vm_page_free() and NO pv_list/wire_count/ref_count guard. vm_page_free() (sys/vm/vm_page.c:562-609) only pv_remove_all()s the page (frees pv_entry structs, does NOT clear any hardware PTE), force-zeroes ref_count=0 (line 600), and returns the frame to the buddy allocator via vm_phys_free_page(). It never walks the page tables.

The in-place COW path hits this exactly as claimed. In vm_fault.c step 5 (line 340-379), when obj==first_obj &&…

### Write to PROT_READ / PROT_NONE private mapping silently granted (protection bypass)
- **Location:** `sys/vm/vm_fault.c:160`  ·  logic  ·  verdict **CONFIRMED**
- **Subsystem:** vm map & fault
- **Defect:** vm_fault decides whether a write fault is legal, and what PTE protection to install, using entry->max_protection rather than the mapping's CURRENT entry->protection. sys_mmap (vm_syscalls.c:470) unconditionally sets max_protection = VM_PROT_READ|WRITE|EXEC for every mapping (so a later mprotect can raise access). As a result the write-fault path treats ANY private (non-VM_INHERIT_SHARE) mapping as writable regardless of its requested prot. In the protection gate at lines 160-164, a write fault whose permission is missing from entry->protection is NOT rejected: the inner test `((prot & VM_PROT_WRITE)==0 || inheritance==VM_INHERIT_SHARE)` is false for a private write fault, so it falls through and services the fault. Then at lines 383-387 enter_prot gets VM_PROT_WRITE OR'd in because `entry->max_protection & VM_PROT_WRITE` is always true, and the downgrade at 388-391 does not strip it for the common obj==first_obj, ref_count==1 case. The page is entered writable. The pmap_fault COW path (pmap.c:2211+) exhibits the same behavior for an already-present read-only page. Net effect: userspace cannot hold read-only or PROT_NONE memory against writes on any MAP_PRIVATE mapping until/unless something re-downgrades it — POSIX requires SIGSEGV. (Read and exec faults on under-protected pages ARE correctly rejected via the `(prot & VM_PROT_WRITE)==0` branch; only writes leak.)
- **Failure scenario:** p = mmap(NULL, 4096, PROT_READ, MAP_PRIVATE|MAP_ANONYMOUS, -1, 0); *(volatile char*)p = 1;  -> first-touch write fault: pmap_fault returns 0 (page not present), vm_fault runs, outer check (protection&WRITE)!=WRITE true, inner check false (write, not shared) so it proceeds, zero-fills, enter_prot = READ|WRITE (max_protection has WRITE) -> page mapped writable, store succeeds instead of SIGSEGV. Same for mmap(PROT_NONE) and for MAP_PRIVATE read-only file mappings (COW copy entered writable).
- **Verifier:** Read sys/vm/vm_fault.c (lines 110-416), sys/vm/vm_syscalls.c (388-471), sys/arch/i386/idt.c (285-323), and the constant defs. The bug is real and triggerable exactly as described.

sys_mmap hardcodes entry->max_protection = VM_PROT_READ|WRITE|EXEC (vm_syscalls.c:470) for every mapping, while entry->protection tracks the requested prot (lines 388-390). For mmap(PROT_READ, MAP_PRIVATE|MAP_ANON): protection=0x01, max_protection=0x07, inheritance=VM_INHERIT_COPY(1).

A write fault arrives as fault_prot=0x03 (idt.c:320-321). In vm_fault: the max_protection gate at line 157 passes (0x07&0x03==0x03).…


## MEDIUM

### AF_UNIX SOCK_DGRAM datagram > 65535 bytes truncates its 2-byte length header, corrupting frame boundaries
- **Location:** `sys/net/af_unix.c:602`  ·  logic  ·  verdict **CONFIRMED**
- **Subsystem:** BSD sockets
- **Defect:** Datagrams are framed in the peer rx ring as [u16 len][payload]. afunix_node_write() (line 602) and afunix_dgram_deliver() (line 1457) only reject sizes with 'size + 2 > AFUNIX_BUF_SIZE' (262144), then store the length as hdr[2] = {size>>8, size&0xFF} — a 16-bit field — while writing the FULL 'size' bytes of payload via afbuf_write(). Any datagram between 65536 and ~262142 bytes therefore records a truncated length (size & 0xFFFF) but deposits the full payload. The reader (afunix_node_read DGRAM path, line 512: mlen = (hdr[0]<<8)|hdr[1]) consumes only the truncated count, leaving the remaining payload bytes in the ring to be misinterpreted as the next datagram's [len][payload] header. Additionally the 'size + 2' test is an integer-overflow-unsafe bounds check (size is size_t from the user len). SO_SNDBUF is reported as 192 KiB, so callers legitimately attempt such sizes.
- **Failure scenario:** Process A does sendto(dgram_fd, buf, 70000, ...); size+2=70002 < 262144 so it is accepted; header stores 70000 & 0xFFFF = 4464 while all 70000 payload bytes are written. Process B's recv() returns a 4464-byte 'datagram', and the leftover 65536 bytes are parsed as bogus subsequent datagrams -> permanent framing desync / data corruption on that socket. With len ~ SIZE_MAX the size+2 check also overflows and is bypassed entirely.
- **Verifier:** Read sys/net/af_unix.c directly. The defect is real and reachable. (1) Arithmetic: afunix_node_write DGRAM branch accepts any size with `size + 2 > AFUNIX_BUF_SIZE` (line 584, AFUNIX_BUF_SIZE=262144, line 97) but stores the length in a 2-byte header `{(uint8_t)(size>>8), (uint8_t)(size&0xFF)}` (line 602) while afbuf_write deposits the FULL payload (line 604). For size=70000 the header truncates to 70000&0xFFFF=4464. The reader (lines 510-520) decodes mlen=(hdr[0]<<8)|hdr[1]=4464 and consumes exactly mlen payload bytes (copy + drop-tail loop), leaving 65536 stale bytes in the ring to be mispars…

### FreeBSD getrusage/getitimer/wait4 native handlers overrun 8-byte-timeval structs
- **Location:** `sys/exec/perso/perso_freebsd.c:107`  ·  abi-correctness  ·  verdict **CONFIRMED**
- **Subsystem:** BSD/foreign personalities
- **Defect:** Same 16-byte-vs-8-byte timeval ABI mismatch as gettimeofday, applied to structs that embed timevals. FREEBSD_SYS_getrusage->sys_getrusage copies out a native struct rusage whose two leading timevals are 16 bytes each (32 bytes) into FreeBSD's rusage whose timevals are 8 bytes each (16 bytes) — an ~16-byte overrun plus every field after ru_utime/ru_stime lands at the wrong offset (garbage maxrss/faults). Same defect for FREEBSD_SYS_getitimer/setitimer (line 89-90; struct itimerval = two timevals, native 32 bytes vs FreeBSD 16) and FREEBSD_SYS_wait4 (line 31; sys_wait4 copyout of native rusage when the caller passes a non-NULL rusage). None of these have a FreeBSD marshalling wrapper the way NetBSD does.
- **Failure scenario:** A FreeBSD binary calls getrusage(RUSAGE_SELF, &ru): kernel copyout writes 88 bytes of native-layout rusage into the 72-byte FreeBSD struct — overrunning adjacent user memory and yielding misaligned/garbage ru_maxrss and cpu-time fields (ru_stime read from where native ru_utime.tv_usec sits).
- **Verifier:** The dispatch table in sys/exec/perso/perso_freebsd.c maps FREEBSD_SYS_wait4 (line 31), setitimer/getitimer (89-90), and getrusage (107) directly to native sys_* handlers with no marshalling wrapper — in contrast to the NetBSD personality, which has netbsd_sys_getrusage/netbsd_sys_wait450 wrappers for exactly these calls. Native time_t and suseconds_t are both int64_t (sys/include/sys/types.h:47,65), making native timeval 16 bytes, itimerval 32 bytes, and struct rusage 88 bytes. sys_getrusage (sys/pm/rusage.c:310) does copyout(sizeof(struct rusage))=88 bytes; sys_wait4 (sys/pm/wait.c:297) copie…

### Unsynchronized global ELF image cache — torn reads under preemption
- **Location:** `sys/exec/formats/elf.c:283`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** ELF loaders & exec
- **Defect:** elf_get_image_info() reads and writes the process-global elf_image_cache[] array and elf_image_cache_hand counter with no lock (elf.c:282-302). A cache hit copies an ~8 KB elf_image_info_t struct out with '*image = elf_image_cache[i].image', and the miss path writes a slot chosen by 'elf_image_cache_hand++ % SIZE'. Since exec no longer suppresses preemption (exec_pin_current_thread only binds the CPU, per exec.c:70-88), a timer preempt during the multi-KB struct copy can let another exec'ing thread evict/overwrite the same slot, yielding a torn image (mismatched ehdr.e_phnum vs phdrs[] vs interp_path/at_phdr). The non-atomic hand increment can also lose updates. Downstream elf_load re-validates per-segment vaddr/size bounds so this is unlikely to be directly memory-unsafe, but it can load the wrong program image or produce inconsistent AT_PHDR/AT_PHNUM auxv.
- **Failure scenario:** Two threads concurrently execve() different binaries; one is preempted mid-copy of a cache entry while the other rewrites that slot -> the first thread executes with a phdr array/phnum/interp path stitched from two different binaries, causing a wrong or failed load with attacker-influenceable metadata.
- **Verifier:** Read sys/exec/formats/elf.c lines 40-305, sys/exec/exec.c lines 58-103, and sys/kern/syscall.c kern_execve (3418-3433). The claim checks out on every point: (1) elf_image_cache[16] and elf_image_cache_hand (elf.c:66-67) are file-static with no lock anywhere in elf.c; the hit path does '*image = elf_image_cache[i].image' (line 284) and the miss path does 'elf_image_cache[elf_image_cache_hand++ % ELF_IMAGE_CACHE_SIZE]' plus 'entry->image = *image' (lines 294-301). (2) elf_image_info_t embeds Elf32_Phdr phdrs[256] = 8 KB (line 40), so both copies are multi-KB memcpys. (3) exec_pin_current_thread …

### Use-after-free of child PCBs in tcp_close() LISTEN teardown
- **Location:** `sys/net/tcp.c:1176`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** TCP/IPv4
- **Defect:** When a listening socket is closed, tcp_close() (LISTEN case) walks g_tcp_pcbs under tcp_lock() and, for every child, sets q->detached=1 and calls tcp_kill_pcb(q, ECONNRESET) which sets q->state=TCP_CLOSED — while leaving the child linked in g_tcp_pcbs. It records the still-live children in rst_list[] and then DROPS THE LOCK (tcp_unlock(f), line 1175) before sending the RSTs via tcp_send_ctl(rst_list[i], ...) at lines 1176-1177 (the unlock is required because tcp_xmit_raw->ip4_output may ARP-wait). But a detached PCB in state TCP_CLOSED is exactly what the retransmit-timer reaper (tcp_timer_tick) frees immediately: `if (p->state==TCP_CLOSED){ if(p->detached){ tcp_free(p); continue; } }`. The reaper is an ordinary preemptible kthread (kthread_create, PID 0) woken every ~250ms. If it wakes and runs in the gap between tcp_unlock() and/or between RST-send iterations, it kfree()s the child PCB, and the subsequent tcp_send_ctl(rst_list[i]) dereferences freed kernel heap (reads q->lport/rport/snd_nxt/rcv_nxt/laddr/raddr inside tcp_xmit_raw).
- **Failure scenario:** A server has a listening TCP socket with one or more established-but-not-yet-accepted (or SYN_RECEIVED) child connections. Thread A closes the listener fd -> afinet_node_close -> tcp_close() LISTEN path: children are marked detached + TCP_CLOSED under the lock, then the lock is released. Thread A is preempted after tcp_unlock() (IRQs now enabled). The tcp 'tcpretx' timer kthread, previously asleep, wakes on its period, runs tcp_timer_tick, finds each child CLOSED+detached and tcp_free()s it (kfree of the tcp_pcb_t and its rxbuf). Thread A resumes and executes tcp_send_ctl(rst_list[i], TCP_RST|TCP_ACK) on the freed pointer -> reads/uses freed heap -> corrupted RST contents or a fault if the page was reused/unmapped; with kmalloc poisoning this can panic.
- **Verifier:** The code matches the claim at every step. sys/net/tcp.c:1160-1177: under tcp_lock() each child of the closing listener gets detached=1 and tcp_kill_pcb() -> state=TCP_CLOSED (line 303; kill "Never frees", children stay linked in g_tcp_pcbs), pointers are saved in rst_list[], then tcp_unlock(f) at 1175 precedes the tcp_send_ctl(rst_list[i], TCP_RST|TCP_ACK) loop at 1176-1177. tcp_timer_tick (343-349) frees exactly that state on its next pass: `if (p->state==TCP_CLOSED) { if (p->detached) { tcp_free(p); continue; } }` with no grace tick, and tcp_free (760-780) kfree()s the PCB and rxbuf. tcp_loc…

### xHCI slot, device/input contexts and transfer rings leaked on every device disconnect
- **Location:** `sys/drivers/usb/xhci.c:281`  ·  resource-leak  ·  verdict **CONFIRMED**
- **Subsystem:** USB stack
- **Defect:** xhci_free_slot() (which issues Disable Slot, clears dcbaa[slot], and frees dev_ctx/in_ctx/ep_ring DMA) is invoked ONLY from the two failure paths inside xhci_setup_slot (lines 325, 358). The USB core's usb_disconnect_device (usb.c:855) has no HCD-level disconnect notification — it only calls the class driver's .detach. Consequently, when a successfully-enumerated xHCI device is unplugged, its slot is never disabled, hc->dcbaa[slot] is never cleared, and all per-slot DMA allocations (32*ctx_size dev_ctx, 33*ctx_size in_ctx, every ep_ring[dci] segment, and any stream_ctx/stream_ring arrays allocated in xhci_ensure_ep) are leaked. hc->addr_slot[addr] is also never cleared. Over repeated unplug/replug cycles this exhausts the controller's 16 slots (XHCI_MAX_SLOTS) and leaks DMA-coherent memory; a re-enumerated device that lands on the same USB address before SET_ADDRESS overwrites addr_slot[] can transiently route via a stale slot's device context.
- **Failure scenario:** Attach and enumerate a USB3 mass-storage device on the xHCI controller (slot allocated, rings + contexts DMA-allocated), then unplug it. Repeat ~16 times: each cycle leaks one slot's contexts+rings and never issues Disable Slot, eventually returning cc!=SUCCESS from Enable Slot (slot exhaustion) so no further xHCI devices enumerate, plus cumulative DMA-coherent memory leak.
- **Verifier:** The core leak claim is real and I verified every link in the chain by reading the code. (1) sys/drivers/usb/xhci.c:281 `xhci_free_slot()` is the ONLY teardown for a slot — it issues TRB_DISABLE_SLOT, clears hc->dcbaa[slot], and dma_free_coherent's dev_ctx (32*ctx_size), in_ctx (33*ctx_size) and ep_ring[1]. Grepping the whole file shows exactly two call sites, lines 325 and 358, both failure paths inside `xhci_setup_slot()` — the [DRV-19] comment even says "Every failure path in xhci_setup_slot funnels here", i.e. it was written only for enumeration failures, never for disconnect. (2) The HCD i…

### Hub-downstream devices are never disconnected or quiesced on hot-unplug
- **Location:** `sys/drivers/usb/usb.c:840`  ·  resource-leak  ·  verdict **CONFIRMED**
- **Subsystem:** USB stack
- **Defect:** usb_root_device_on_port() only matches devices with d->parent == NULL, and usb_hotplug_scan() iterates just the root-hub ports of each HCD. Devices enumerated behind an external/virtual USB hub are recorded with parent == hub (usb_enumerate_device_parent, set at usb.c:651) so they are invisible to the disconnect scan. The hub class driver (usb_hub.c) enumerates its downstream ports exactly once in usb_hub_attach->usb_hub_enumerate_ports and has no periodic port-status rescan, and its usb_hub_detach does nothing to its children. Therefore unplugging any device attached through a hub never runs that device's class-driver .detach (no SCSI force-unmount / DMA-buffer free / thread quiesce) and never frees its usb_device_t or returns its USB address to the bitmap — a permanent leak, and for usb_msc a stale scsi_link/blockdev referencing a device that is physically gone.
- **Failure scenario:** qemu presents more USB devices than root ports so a virtual hub (0409:55aa) is created; a USB stick enumerates behind it (parent=hub). Unplug the stick: usb_hotplug_scan never sees it (parent != NULL), usb_msc_detach never runs, the mounted filesystem is not force-unmounted, DMA buffers and the usb_device_t leak, and the USB address is never freed.
- **Verifier:** Verified directly in the source. usb.c:840-848 (usb_root_device_on_port) matches only d->parent == NULL, and usb_hotplug_scan (usb.c:879-906) polls only root-hub ports via hcd->port_status; usb_disconnect_device's sole caller is that scan (usb.c:892). Hub-attached devices are tagged with parent = hub->udev at usb_hub.c:193 (comment [DRV-04] says this is specifically so the root scan skips them). usb_hub.c enumerates downstream ports exactly once from usb_hub_attach (line 261), has no status-change interrupt endpoint or periodic rescan anywhere, and usb_hub_detach (lines 266-278) only zeroes it…

### Legacy fs op-dispatch helpers return bare -1, surfacing to userspace as EPERM
- **Location:** `sys/vfs/vfs.c:954`  ·  error-handling  ·  verdict **CONFIRMED**
- **Subsystem:** VFS & buffer cache
- **Defect:** The op-dispatch wrappers return a bare -1 (not a negative errno) when the backing fs_node method is absent: unlink_fs (954), rmdir_fs (961), rename_fs (968), link_fs (947), symlink_fs (940), readlink_fs (933), statfs_fs (975), mknod_fs (1084), and mmap_fs. These -1 values are propagated straight to userspace by the syscall layer — e.g. kern_unlinkat() returns `unlink_fs(parent,file)` (syscall.c:2537), kern_rename() returns `rename_fs(...)` (2571), sys_statfs returns `statfs_fs(...)` (2590), and kern_open_from() returns mknod_fs()'s result for O_CREAT (syscall.c:644-648). libc negates the kernel return into errno, so a bare -1 becomes errno=1=EPERM regardless of the real cause. This is the project's documented bare-`-1` errno-contract class (kernel must return -errno). Correct values would be e.g. -EROFS/-EPERM for unlink/rename on an op-less or read-only fs, -EINVAL for readlink on a node type without the op, and -ENOSYS/-EOPNOTSUPP for statfs.
- **Failure scenario:** statfs(2) on a path whose backing fs_node has no ->statfs op (e.g. a pseudo-fs node) -> statfs_fs() returns -1 -> userspace sees errno=EPERM ("Operation not permitted") instead of the correct ENOSYS/ENOTSUP. Likewise unlink()/rename()/link() on a filesystem lacking that op, or open(...,O_CREAT) on a directory whose fs has no mknod op, all report EPERM instead of EROFS/EOPNOTSUPP — misleading POSIX callers and conformance tests.
- **Verifier:** Read the code directly. sys/vfs/vfs.c really does `return -1;` from readlink_fs (933), symlink_fs (940), link_fs (947), unlink_fs (954), rmdir_fs (961), rename_fs (968), statfs_fs (975), mknod_fs (1084) when the fs_node op is NULL (mmap_fs returns (void*)-1 at 1091), while neighboring helpers in the same file correctly return -EINVAL/-ENOENT — so this is a contract violation, not local convention. sys/kern/syscall.c propagates these raw: kern_unlinkat returns unlink_fs() (2537), kern_rename returns rename_fs() (2571), kern_statfs/kern_fstatfs return statfs_fs() (2590/2606), kern_open_from retu…

### Lazy-FPU owner is a single global, not per-CPU
- **Location:** `sys/arch/i386/fpu/fpu_emu.c:20`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** arch/i386 traps & context
- **Defect:** `static struct process *fpu_owner` is a single kernel-global tracking which process's x87/SSE registers are live in *the* hardware FPU. CR0.TS (the trap trigger) is per-CPU hardware state, but fpu_owner is shared across all CPUs. fpu_switch() (arch_switch_to) sets TS on the running CPU, and fpu_handler() (#NM) reads/writes the shared fpu_owner and issues fnsave/fxsave against whichever process the single global names. On a real SMP boot two CPUs each run FPU-using processes concurrently; the ownership bookkeeping and the save/restore target are not per-CPU, so one CPU's #NM handler will save the wrong (other CPU's) live registers into another process's fpu_ctx and load stale state. Currently latent only because APs park in hlt and never schedule (per project notes), but the lazy-FPU model is structurally unsafe the moment SMP scheduling is enabled.
- **Failure scenario:** SMP enabled; CPU0 runs process A (fpu_owner=A) and CPU1 takes an #NM for process B. fpu_handler on CPU1 sees fpu_owner==A, executes fpu_save_context(A) which fnsave/fxsaves CPU1's live FPU registers (B-ish/garbage) into A's save area, then sets fpu_owner=B. A's saved x87/SSE state is corrupted; when A next faults in, fpu_restore_context(A) loads corrupt data -> wrong FP results / SIGFPE.
- **Verifier:** Every element of the claim checks out against the actual code. (1) /home/segin/substrate/sys/arch/i386/fpu/fpu_emu.c:20 is exactly `static struct process *fpu_owner = NULL;` — one kernel-wide global, no lock, and a grep confirms no per-CPU FPU ownership state exists anywhere else (percpu has none; the only other state is the per-process `fpu_ctx` in sys/include/sys/proc.h:94-97,209). (2) `current_process` IS per-CPU — sys/include/sys/proc.h:577 `#define current_process (*curproc_slot())` — so in fpu_handler (fpu_emu.c:82-123) a per-CPU value is compared against and assigned into the shared glo…

### ext2_free_inode mutates node cache without ext2_node_cache_lock
- **Location:** `sys/fs/ext2/ext2.c:2683`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** ext2 filesystem
- **Defect:** ext2_free_inode() walks ext2_node_cache[], and for each matching unpinned slot kfree()s block_buf/indirect_buf/dindirect_buf/tindirect_buf and zeroes both the ext2_node_t and ext2_fs_node_cache entry — all before it takes any lock (fs->alloc_lock is taken later at line 2717, and it is a different lock anyway). ext2_alloc_node() was specifically given ext2_node_cache_lock to make slot selection + population atomic, but ext2_free_inode bypasses that lock entirely while touching the same slots. On SMP (or on UP if alloc_node sleeps in ext2_read_inode I/O between selecting a slot and populating it) the two can operate on the same slot concurrently.
- **Failure scenario:** Thread A in ext2_alloc_node holds ext2_node_cache_lock, selects an unpinned slot, and is in the 'clean up old context' kfree() / memcpy() sequence. Thread B (unlink/rmdir/mknod error path) enters ext2_free_inode, matches the same inode_num with pin_count==0, and kfree()s that slot's block_buf and memsets the slot — double-free of block_buf or use of a freed/half-populated slot, corrupting the node cache.
- **Verifier:** The finding is accurate on every point I could check against the source.

1. The unlocked mutation is real. sys/fs/ext2/ext2.c:2683-2710 (ext2_free_inode) walks ext2_node_cache[] and, for each slot matching (fs, inode_num, pin_count==0), kfree()s block_buf/indirect_buf/dindirect_buf/tindirect_buf, NULLs ctx->fs/inode_num, and memset()s the paired ext2_fs_node_cache[i] — with no lock held. The only lock in the function, fs->alloc_lock, is taken later at line 2717 and guards only the inode bitmap, not the node cache.

2. The lock it bypasses exists precisely for this data. The comment block at l…

### virtio-blk synchronous read races shared descriptor ring on concurrent callers
- **Location:** `sys/drivers/virtio/virtio_blk.c:155`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** input/audio/virtio/devices
- **Defect:** virtio_blk_read_sectors() hard-codes descriptor indices id0=0,id1=1,id2=2 in the single global vblk ring and drives a shared last_used_idx poll loop with no lock, explicitly 'assuming single-threaded, sync' (comment at line 146). The geom layer that calls it (kern/geom/geom_subr.c geom_read/geom_read_sectors -> disk->read) takes no lock either. On SMP, or when two threads both miss the block cache and issue reads concurrently, both overwrite descriptors 0-2 of the same ring, publish into avail->ring[idx], and share last_used_idx — so one request's data buffer descriptor / status byte is clobbered by the other, and the poll loop can consume the wrong completion. Result: wrong or corrupted sector data returned to one caller, or a stuck poll.
- **Failure scenario:** Two CPUs (or two threads) call virtio_blk_read_sectors for different LBAs at the same time. Thread B rewrites vblk.desc[1].addr/len to its own buffer while Thread A is mid-transfer; the device DMAs A's sectors into B's buffer (or vice versa) and the single last_used_idx counter mis-attributes completions -> silent data corruption / filesystem misread on a virtio-blk root.
- **Verifier:** The defect is exactly as described and no layer above prevents it. sys/drivers/virtio/virtio_blk.c:144-197: a single global `vblk` struct holds the ring and `last_used_idx`; virtio_blk_read_sectors() hard-codes descriptors 0/1/2 (lines 155-157), publishes into avail, and busy-polls `while (vblk.last_used_idx == vblk.used->idx) pause;` with no mutex/spinlock/intr_disable/preemption-disable — the comment at line 146 literally says "assuming single-threaded, sync". Callers do not serialize it: geom_subr.c:74-102 (geom_read/geom_read_sectors/geom_part_read) are bare pass-throughs to disk->read; bl…

### UDF udf_add_fid: only one sector read but directory treated as 4096 bytes
- **Location:** `sys/fs/udf/udf_write.c:782`  ·  logic  ·  verdict **CONFIRMED**
- **Subsystem:** minix/udf/9p/sysv/procfs
- **Defect:** udf_add_fid kmallocs a 4096-byte dir_buf but reads only UDF_SECTOR_SIZE (2048) bytes from disk (line 782), then uses dir_size = dir_fe->info_length (bounded only to <=4096 at line 792) and writes the new FID at dir_buf+dir_size. When dir_size is in (2048,4096], the FID is placed into the never-read upper half (uninitialised kmalloc memory), the tag checksum is computed over surrounding garbage, and only 2048 bytes are written back (line 825) so the new entry past offset 2048 is never persisted while dir_fe->info_length is still advanced. Multi-sector directories are silently corrupted and uninitialised heap is hashed/stored.
- **Failure scenario:** mkdir/create enough entries in a UDF directory to push info_length past 2048; the next udf_add_fid writes the FID into uninitialised heap, bumps info_length, but persists only the first sector → lost directory entry and uninitialised-data-derived checksum on disk.
- **Verifier:** udf_add_fid (sys/fs/udf/udf_write.c:775-828) kmallocs 4096 bytes but reads only UDF_SECTOR_SIZE=2048 (udf.h:15) at line 782, bounds-checks the new entry against 4096 (line 792), builds the FID at dir_buf+dir_size (line 799), advances dir_fe->info_length unconditionally (line 822), then writes back only UDF_SECTOR_SIZE bytes (line 825). Once info_length exceeds 2048 (~47 mkdirs at ~44 bytes/entry via udf_vfs_mkdir, udf.c:697), the new FID lies entirely past the persisted region: the call returns success and grows info_length but the entry never reaches disk — silent lost entries; the straddle c…

### sched_get_thread returns unreferenced pointer; lwp ops write to possibly-freed thread_t
- **Location:** `sys/pm/sched.c:960`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** power/pm subsystem
- **Defect:** sched_get_thread() looks up a thread_t under tid_lock but drops the lock before returning the raw pointer (line 554-557). Callers sched_lwp_detach/sched_lwp_suspend/sched_lwp_continue/sched_lwp_wait_check/sched_set_priority then read-modify-write t->flags/t->state with no lock and no reference, racing the reapers that free thread_t storage (sched_reap_thread, sched_reap_any_zombie_sibling, detached-zombie sweep).
- **Failure scenario:** Thread X of a NetBSD-personality process exits (THREAD_ZOMBIE). Thread A calls _lwp_wait(0) → sched_reap_any_zombie_sibling() claims, unlinks and kfree()s X, while thread B concurrently calls _lwp_detach(X_tid): B's sched_get_thread returned X before A's unlink, then B executes `t->flags |= THREAD_F_DETACHED` on freed memory — a write-after-free that corrupts whatever the slab was reallocated to.
- **Verifier:** Read sys/pm/sched.c directly. sched_get_thread (lines 551-557) acquires tid_lock, looks up the tid, releases the lock, and returns the raw thread_t* with no reference taken (thread_t has no refcount; sched_storage_free at 94-96 is plain kfree). sched_lwp_detach (956-964) then dereferences it unlocked: `t->proc != current_process` and `t->flags |= THREAD_F_DETACHED` (line 960); sched_lwp_suspend/continue/wait_check/sched_set_priority share the pattern. Concurrently, sched_reap_any_zombie_sibling (1013-1034) claims a ZOMBIE non-detached sibling via sched_unlink_locked under tid_lock, drops the l…

### pgrp/session syscalls return bare -1 instead of -errno
- **Location:** `sys/pm/pgrp.c:278`  ·  error-handling  ·  verdict **CONFIRMED**
- **Subsystem:** power/pm subsystem
- **Defect:** sys_setsid (lines 238, 243, 249), sys_getsid (278, 282), sys_getpgid (328, 330), and sys_setpgid (352, 354, 372, 376, 386, 394, 397, 401) all return bare -1 on error, with comments annotating the intended errno (/* ESRCH */, /* EINVAL */, /* ENOMEM */, /* EPERM */). The kernel errno contract is -errno; -1 == -EPERM, so every failure surfaces as EPERM. getsid/getpgid on a nonexistent pid must set ESRCH, setpgid with pgid<0 must set EINVAL, setsid on ENOMEM must set ENOMEM — all report EPERM instead, misleading shells' job-control error handling and any POSIX conformance test of these calls.
- **Failure scenario:** getpgid(99999) with no such pid returns -1 → libc sets errno=EPERM; a conforming caller checking for ESRCH ('no such process') mis-classifies the error. setpgid(pid,-5) yields EPERM instead of the required EINVAL.
- **Verifier:** Read sys/pm/pgrp.c directly: every cited line returns bare -1 with a comment naming the intended errno, e.g. sys_getpgid line 328 `if (!target) return -1; /* ESRCH */`, sys_setpgid line 354 `if (pgid < 0) return -1; /* EINVAL */`, sys_setsid line 243 `if (!sess) return -1; /* ENOMEM */`. The kernel's -errno contract is proven inside the same file — sys_setlogin (lines 302/305) correctly returns -EFAULT/-EPERM — and by the project convention that libc negates the kernel return into errno, so -1 surfaces as errno 1 = EPERM. Dispatch is a direct pointer table (perso_native.c:199-202 plus the Free…

### fork failure returns bare -1 → userspace sees EPERM instead of ENOMEM/EAGAIN
- **Location:** `sys/pm/process.c:480`  ·  error-handling  ·  verdict **CONFIRMED**
- **Subsystem:** power/pm subsystem
- **Defect:** proc_fork_common() returns -1 for every failure (proc_create OOM at 480, pmap_fork at 490, vm_map_fork at 503, ldt_clone at 524), and only propagates sched_fork_thread's code at 644. arch/i386/syscall.c:617 returns sched_fork_process()'s value directly to userspace, so a fork that fails from memory exhaustion reports errno EPERM (-1) rather than ENOMEM or EAGAIN, which POSIX requires and which retry loops in shells/daemons key on.
- **Failure scenario:** Under memory pressure pmap_fork() returns NULL; fork(2) fails with errno=EPERM. A shell or server that treats EAGAIN as 'retry later' but EPERM as fatal permission failure aborts instead of retrying; OPTS fork errno tests fail.
- **Verifier:** Read sys/pm/process.c proc_fork_common (line 478): all four resource-failure paths return bare -1 (line 480 proc_create NULL, 491 pmap_fork NULL, 503 vm_map_fork NULL, 524 ldt_clone_process failure). The one path that propagates a code (line 644, sched_fork_thread) is no better: sys/arch/i386/sched.c:94 sched_fork_thread also returns bare -1 when sched_alloc_thread or the 16KiB kstack pmm_alloc_contiguous(4) fails. Dispatch is a straight pipe: syscall.c:637 sys_fork → arch_fork_with_stack → line 617 return sched_fork_process(...) → proc_fork shim (process.c:723) → proc_fork_common; no layer re…

### Non-atomic f_count++ during fork races sibling-thread close
- **Location:** `sys/pm/process.c:563`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** power/pm subsystem
- **Defect:** proc_fork_common duplicates the parent's fd table with plain `child_proc->fds[j]->f_count++` (and `--` in the error path at line 622) under no lock. f_count is manipulated with unlocked ++/-- everywhere (kern/syscall.c:942 `f->f_count--; if (f->f_count <= 0) free`), so in a multithreaded parent, fork on one CPU racing close()/dup() of the same file_t on another CPU can lose an increment or decrement of the shared refcount.
- **Failure scenario:** Thread A of a threaded process calls fork() while thread B close()s a shared fd: A's f_count++ and B's f_count-- interleave non-atomically; a lost increment leaves f_count one too low, so the last close frees the file_t while the child still holds fds[j] pointing at it → use-after-free on the child's next read/write; a lost decrement leaks the open file description permanently.
- **Verifier:** Verified in the code: sys/pm/process.c:559-563 does `child_proc->fds[j] = parent->fds[j]; child_proc->fds[j]->f_count++;` (and bare `f_count--` at 620-625 in the error path) with no lock and no preempt/intr disable anywhere in proc_fork_common around the loop. f_count is a plain uint32_t (sys/include/sys/file.h:49); struct file has no lock, and a tree-wide grep finds no fd-table lock of any kind. The close path (file_close_ptr, sys/kern/syscall.c:942) does unlocked `f->f_count--; if (f->f_count <= 0) ... file_free(f)`, and kern_close frees before clearing fds[fd]. Threads genuinely share the t…

### MBR extended-partition (EBR) chain parse can loop forever on crafted media
- **Location:** `sys/kern/geom/geom_mbr.c:63`  ·  logic  ·  verdict **CONFIRMED**
- **Subsystem:** storage drivers
- **Defect:** parse_extended() walks the EBR linked list with the loop guard `while (ebr_lba < ext_start + ext_size && logical_count < 60)`. logical_count is only incremented when entry[0] is a non-empty logical partition (lines 79-110). The link to the next EBR (entry[1]) is followed whenever it is an extended type, setting ebr_lba = ext_start + entry->lba_start with no visited-set, minimum-progress, or independent iteration cap. A crafted EBR chain whose entry[0] is always empty (so logical_count never advances) but whose entry[1] points to an in-range EBR that cycles back keeps ebr_lba < ext_start+ext_size indefinitely, so the loop never terminates.
- **Failure scenario:** A malicious USB stick / disk image presents an MBR with an extended partition whose EBR at LBA X has entry[0].type=0 (empty) and entry[1] pointing (relative) back to LBA X (or a 2-node cycle). geom_mbr_sniff -> parse_extended spins forever re-reading the same EBR sector at partition-scan time, hanging the thread doing device registration (DoS on plugging in the device).
- **Verifier:** Read sys/kern/geom/geom_mbr.c:55-125 (parse_extended) and the supporting definitions. The loop guard at line 63 is `while (ebr_lba < ext_start + ext_size && logical_count < 60)`. Two independent facts make the finding real:

1. logical_count (line 61) is incremented ONLY inside the `if (entry->type != 0 && entry->lba_size > 0)` block for entry[0] (line 79, increment at line 110). If a crafted EBR keeps entry[0].type == 0, logical_count stays 0 forever, so the `logical_count < 60` half of the guard never fires.

2. The next-EBR link is followed at lines 113-121: when entry[1] is GEOM_MBR_EXTEND…

### rwlock waiting_writers leaks permanently when a parked writer is killed
- **Location:** `sys/kern/rwlock.c:89`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** sync primitives
- **Defect:** rw_wlock increments rw->waiting_writers before its sleep loop and only decrements it after successfully acquiring the lock. proc_exit (sys/pm/process.c:1671-1680) removes parked sibling threads from their sleepqs and marks them THREAD_ZOMBIE — such a thread never resumes rw_wlock and never decrements waiting_writers. Because rw_rlock and rw_try_rlock refuse readers while waiting_writers != 0 (writer preference, lines 28 and 39), a leaked count blocks all future readers forever, and nothing ever heals the counter. rwlocks are used by the global VFS name cache and mount tables (vfs_cache.c, vfs_mount.c), so the leak can be on a system-wide lock.
- **Failure scenario:** Multi-threaded process: thread B blocks in rw_wlock on the global namecache rwlock; thread A calls exit_group (or the process takes a fatal signal). proc_exit zombifies B while parked -> waiting_writers stays >0 forever. Every subsequent path lookup's rw_rlock on the namecache lock sleeps in the 'waiting_writers != 0' loop with no writer left to acquire/release -> system-wide filesystem hang.
- **Verifier:** The leak mechanism is real and unguarded: rw_wlock (sys/kern/rwlock.c:89-97) increments waiting_writers before parking via sleepq_add/sched_yield and only decrements after acquiring. proc_exit (sys/pm/process.c:1671-1680) calls sleepq_remove_thread — which only dequeues and clears wait_chan without making the thread runnable (sleepq.c:160-207) — then marks sibling threads THREAD_ZOMBIE; the scheduler (sys/pm/sched.c:418-438) never runs zombies and even re-zombifies resurrected threads of SZOMB processes. Nothing in proc_exit touches rwlock counters (only futex cleanup and mutex_release_owned_b…

### Single LK_WANT_EXCL bit clobbered across concurrent exclusive waiters
- **Location:** `sys/kern/lockmgr.c:143`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** sync primitives
- **Defect:** LK_WANT_EXCL is one shared bit but is set once per waiter before the wait loop and cleared by (a) whichever writer wins (line 143), (b) any failed LK_EXCLUSIVE|LK_NOWAIT trylock (line 122), and (c) a failed/succeeding LK_UPGRADE (lines 172, 191). Waiters never re-assert it inside their loop. Once cleared while other exclusive waiters are still parked, the writer-preference gate in LK_SHARED (line 83) opens and readers stream through, starving the remaining writers indefinitely.
- **Failure scenario:** W1 and W2 both request LK_EXCLUSIVE on a share-locked vnode lock; both set LK_WANT_EXCL. W1 eventually wins and clears the bit; W2 keeps looping on 'sharecount > 0' with LK_WANT_EXCL now clear. A steady stream of shared lockers (e.g., a stat() storm on the same file) keeps sharecount > 0 continuously, so W2 never acquires — unbounded writer starvation. A mere failed trylock (LK_EXCLUSIVE|LK_NOWAIT) from a third thread has the same clobbering effect while W2 waits.
- **Verifier:** Read sys/kern/lockmgr.c in full. LK_EXCLUSIVE sets LK_WANT_EXCL once at line 118 before its wait loop, and the loop body (lines 119-141) never re-asserts it after waking. The bit is cleared unconditionally by three paths while other exclusive waiters may still be parked: the winning writer at line 143 (`lk_flags &= ~LK_WANT_EXCL` on acquisition), a failed LK_EXCLUSIVE|LK_NOWAIT trylock at line 122, and LK_UPGRADE at lines 172/191 (`&= ~(LK_WANT_EXCL|LK_WANT_UPGRADE)`). The LK_SHARED reader gate at line 83 blocks only on (LK_HAVE_EXCL|LK_WANT_EXCL|LK_WANT_DRAIN) and is explicitly commented as "…

### Turnstile pool exhaustion panics the kernel (128 contended locks)
- **Location:** `sys/kern/turnstile.c:131`  ·  resource-leak  ·  verdict **CONFIRMED**
- **Subsystem:** sync primitives
- **Defect:** Turnstiles come from a fixed static pool of TURNSTILE_POOL_SIZE=128 entries (line 31-32) with no dynamic fallback; one turnstile exists per lockmgr lock that currently has a waiter blocked on an exclusively-held lock. turnstile_block panics ('turnstile_block: pool exhausted') when allocation fails (line 131). The process registry is dynamic (no fixed cap) and every vnode has a lockmgr lock, so a sufficiently parallel workload contending >128 distinct vnode/mount/namecache locks simultaneously converts memory-pressure-free load into a full kernel panic (denial of service reachable from unprivileged file system activity).
- **Failure scenario:** A parallel build or torture test with hundreds of processes contending exclusive vnode locks on >128 distinct files at the same instant: the 129th distinct contended lock's waiter calls turnstile_block -> turnstile_alloc returns NULL -> panic, taking down the whole system.
- **Verifier:** sys/kern/turnstile.c is exactly as described: a static 128-entry pool (lines 31-32) with a recycle free list but no dynamic fallback — turnstile_alloc() returns NULL when both are empty (lines 56-69), and turnstile_block() responds with panic("turnstile_block: pool exhausted") (lines 127-133). Tracing the only callers in sys/kern/lockmgr.c: turnstile_block(lkp, lk_lockholder) runs whenever a LK_SHARED (line 90) or LK_EXCLUSIVE (line 128) waiter blocks while an exclusive holder exists, and turnstile_release(lkp) frees the entry only at LK_DOWNGRADE (line 209) or final exclusive LK_RELEASE (line…

### vm_page_free on an already-freed page clears PG_FREE, defeating the buddy double-free guard
- **Location:** `sys/vm/vm_page.c:606`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** vm allocators
- **Defect:** vm_page_free() does m->flags &= PG_PMM_ALLOC (line 606), which strips every flag except PG_PMM_ALLOC — including PG_FREE. If the page is at that moment already sitting on the buddy free list (PG_FREE set, PG_PMM_ALLOC clear), this wipes PG_FREE so the subsequent vm_phys_free_page() sails past its PG_FREE double-free early-return (phys_mem.c:323) and instead hits the 'free of unallocated page' panic; if the frame was meanwhile re-allocated (PG_PMM_ALLOC set again, ref_count=1), the second vm_page_free zeroes the new owner's ref_count and frees the live frame. Nothing serialises the two legitimate freeing paths that can both reach a read-ahead page: pagedaemon reclaim (vm_page_reclaim_inactive_clean dequeues under the queue lock, drops it, then frees) and vm_object_deallocate (detaches the object's page list and frees each page, with no queue-lock coordination beyond vm_page_free's own).
- **Failure scenario:** File-backed mmap workload under memory pressure: pagedaemon dequeues a clean read-ahead page from the inactive queue (clears PG_INACTIVE, drops the lock); the owning process exits concurrently and vm_object_deallocate frees the same page to the buddy allocator, which re-allocates it to another CPU; pagedaemon's pending vm_page_try_to_free -> vm_page_free then strips the new owner's flags/ref_count and buddy-frees the in-use frame -> the frame is handed out twice -> silent corruption (or, without reallocation, a spurious 'vm_phys: free of unallocated page' panic).
- **Verifier:** All three legs of the finding check out against the actual code. (1) sys/vm/vm_page.c:606 does `m->flags &= PG_PMM_ALLOC;`, which strips PG_FREE; a page already on the buddy free list has PG_FREE set (vm_phys_buddy_enqueue, phys_mem.c:83) and PG_PMM_ALLOC clear (phys_mem.c:340), so a second vm_page_free leaves flags==0. (2) vm_phys_free_page's double-free early-return (phys_mem.c:323, `if (page->flags & PG_FREE) return;`) is then defeated; ref_count>1 is false (vm_page.c:600 zeroed it) and PG_PMM_ALLOC is clear, so it hits panic("vm_phys: free of unallocated page") at phys_mem.c:338. If the fr…

### kfree trusts caller-supplied size; cross-zone free reaches the per-CPU bucket unvalidated
- **Location:** `sys/vm/vm_kmem.c:200`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** vm allocators
- **Defect:** kfree(ptr,size) selects the UMA zone purely from the caller's size argument (kmem_zone_index) and calls uma_zfree(kmem_zones[idx], ptr). uma_zfree's only ownership check is uma_find_slab's us_zone comparison, which (a) runs unlocked and (b) protects only the slow slab path: on zone mismatch uma_item_marker returns NULL, the double-free guard is silently skipped, and the fast path (uma_core.c:1027-1046) pushes the foreign item into the WRONG zone's per-CPU free bucket with no validation whatsoever. A later uma_zalloc on that zone returns an item physically sized for the smaller class. Additionally, kfree with size > 4096 on a small UMA pointer takes the large-allocation path and dereferences ptr-8 for the magic check, reading a neighboring slab item; an accidental 0x4C41524D match leads to pmm_free_contiguous on a non-page-aligned interior pointer.
- **Failure scenario:** Any caller that frees with a size in a different power-of-2 bucket than it allocated with (alloc 100 bytes -> kmem-128 zone; free with size 130 -> kmem-256 zone): the 128-byte item enters kmem-256's bucket; a later 256-byte kmalloc receives it and the user writes 256 bytes -> 128-byte overflow into the adjacent slab object -> silent kernel heap corruption with the allocator's own detection machinery bypassed.
- **Verifier:** Read sys/vm/vm_kmem.c and sys/vm/uma_core.c. The corruption chain exists exactly as claimed: (1) kfree (vm_kmem.c:188-200) selects the zone purely from the caller-supplied size via kmem_zone_index and calls uma_zfree(kmem_zones[idx], ptr) — never consulting the pointer's real slab, even though uma_item_size() (used by krealloc at line 267) could recover the true size. (2) uma_find_slab (uma_core.c:683-686) DETECTS the us_zone mismatch but only prints "slab zone mismatch!" and returns NULL; uma_item_marker propagates NULL, and uma_zfree line 999 treats NULL as "bootstrap allocation, skip double…

### Pagedaemon and stats paths iterate page queues without the queue lock
- **Location:** `sys/vm/vm_page.c:872`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** vm allocators
- **Defect:** vm_page_reclaim_inactive_clean (reads m=inactive_queue and next=m->next at lines 871-874 BEFORE taking vm_page_queue_lock), vm_page_launder_inactive_dirty (lines 898-916, no lock at all around the walk), vm_page_age_scan, vm_page_get_stats and vm_page_get_vmstat all traverse the queues unlocked while vm_page_free/activate/deactivate concurrently splice them under the lock. Crucially, vm_page_t.next/prev are the SAME fields the buddy allocator uses for its free lists (vm_phys_buddy_enqueue), so a page freed between the walker's load of m and its load of m->next redirects the walk into the buddy free list, which mutates under a different lock (vm_phys_lock). The under-lock flag re-validation only protects the splice, not the traversal itself.
- **Failure scenario:** Under memory pressure, pagedaemon walks the inactive queue while another CPU's process-exit path frees the current page: m->next now points at the buddy free-list neighbor; the scanner wanders through buddy-owned pages (or a cycle formed by concurrent buddy enqueue/dequeue), producing an unbounded scan (pagedaemon livelock, machine-wide stall) or skipping the pages it was supposed to reclaim.
- **Verifier:** Verified in sys/vm/vm_page.c: vm_page_reclaim_inactive_clean reads m=inactive_queue (line 870) and next=m->next (line 873) before taking vm_page_queue_lock (line 878), and the under-lock re-validation only guards the splice — the stale `next` is followed unconditionally (line 892). vm_page_launder_inactive_dirty (898-916) walks with no lock at all; vm_page_age_scan (1109/1146), vm_page_get_stats (1211-1224), and vm_page_get_vmstat (1237-1247) likewise read m->next unlocked. The field-sharing claim is true: vm_page_t has one next/prev pair (vm_page.h:22-23) and vm_phys_buddy_enqueue (phys_mem.c…

### lockmgr LK_WANT_EXCL stuck set when a parked exclusive waiter is killed
- **Location:** `sys/kern/lockmgr.c:118`  ·  concurrency  ·  verdict **PLAUSIBLE**
- **Subsystem:** sync primitives
- **Defect:** LK_EXCLUSIVE sets lkp->lk_flags |= LK_WANT_EXCL before its wait loop; the flag is cleared only on that waiter's success (line 143) or its own LK_NOWAIT failure. If the waiter thread is destroyed while parked (proc_exit zombifies parked siblings after sleepq_remove_thread, process.c:1671-1680), LK_WANT_EXCL remains set with no owner. LK_SHARED blocks while LK_WANT_EXCL is set (line 83), so all shared acquirers of that lock hang until some future LK_EXCLUSIVE acquisition happens to succeed and clear the flag — which may never occur on a mostly-shared lock. lk_waitcount is similarly leaked (incremented at line 126, never decremented by a killed waiter).
- **Failure scenario:** Thread B of a process waits for an exclusive vnode lock (LK_EXCLUSIVE) held shared by others; thread A exit_group()s, B is zombified while parked. LK_WANT_EXCL stays set on that vnode's lock forever. Every later open/stat that takes the vnode lock LK_SHARED sleeps in the 'while (flags & LK_WANT_EXCL)' loop; the wake_all on the last shared release re-runs the check and re-sleeps. The vnode is permanently inaccessible; processes pile up unkillable in the wait loop.
- **Verifier:** The mechanism is real and verified line-by-line: sys/kern/lockmgr.c sets LK_WANT_EXCL at line 118 before the LK_EXCLUSIVE wait loop, increments lk_waitcount at line 126, sleeps via sleepq_add+sched_yield with lk_interlock dropped, and clears the flag only on the waiter's own NOWAIT failure (line 122) or success (line 143); lk_waitcount-- (line 140) also only runs if the waiter rewakes. proc_exit — actually in sys/pm/process.c:1661-1680, not sys/kern — force-cleans sibling threads with futex_thread_exit and mutex_release_owned_by_thread (mutexes get unwound; lockmgr has NO equivalent), then sle…


## LOW

### getrusage wrappers return bare -1 instead of -errno
- **Location:** `sys/exec/perso/perso_netbsd.c:34`  ·  error-handling  ·  verdict **CONFIRMED**
- **Subsystem:** BSD/foreign personalities
- **Defect:** Several foreign getrusage paths return a bare -1 on the internal error branch instead of a negated errno: netbsd_sys_getrusage (perso_netbsd.c:34 on kern_times failure), netbsd_sys_getrusage50 (netbsd_user.c:1267), and openbsd_sys_getrusage (perso_openbsd.c:53 and :56). libc negates the kernel return into errno, so a bare -1 surfaces to userland as errno=EPERM (1) rather than the intended failure, contradicting the documented kernel contract that syscalls return -errno.
- **Failure scenario:** kern_times()/sys_times() returns (clock_t)-1 inside netbsd_sys_getrusage: the wrapper returns -1, libc sets errno=EPERM, and the NetBSD caller reports 'Operation not permitted' for a resource-usage query that actually hit an internal error.
- **Verifier:** All four cited returns exist verbatim: perso_netbsd.c:34 and netbsd_user.c:1267 both do `if ((clock_t)kern_times(&t) == (clock_t)-1) return -1;`, and perso_openbsd.c:53/:56 return bare -1 for bad `who` and on `sys_times` failure. The dispatcher (sys/arch/i386/syscall.c:534-541) negates any negative BSD-personality return into a positive errno via bsd_errno_xlate and sets CF, so -1 surfaces as errno=EPERM — the contract violation is genuine. Reachability: the NetBSD branches are nearly dead (kern_times at sys/kern/time.c:486 returns -1 only for buf==NULL, impossible here, or when the 64-bit tic…

### execve returns bare -1 (maps to EPERM) instead of correct errno
- **Location:** `sys/exec/formats/elf.c:1531`  ·  error-handling  ·  verdict **CONFIRMED**
- **Subsystem:** ELF loaders & exec
- **Defect:** Several elf_execve failure paths return bare -1, which libc negates into EPERM rather than the correct error: 'not a regular file' returns -1 (should be -EACCES/-ENOEXEC, elf.c:1531), missing root returns -1 (elf.c:1498), pmap_create/vm_map_create failure returns -1 (elf.c:1593/1615), and exec_setup_stack OOM leaves error_code at its -1 default (elf.c:1508,1743) rather than -ENOMEM. This violates the kernel's stated contract (return -errno, not bare -1) and misreports failures to userland.
- **Failure scenario:** execve() of a directory or an image that fails stack setup under memory pressure returns EPERM to the caller instead of EACCES/ENOMEM, so a shell or loader reports 'Operation not permitted' and cannot distinguish permission from resource failures.
- **Verifier:** Read sys/exec/formats/elf.c:1480-1901, sys/exec/exec.c:151-230, sys/kern/syscall.c:3412-3433, and lib/c/src/sys.c:60-66/205-207. Every claimed bare -1 site exists exactly as described: (1) `if (!root) { ... return -1; }` at elf.c:1496-1499; (2) `(file->flags & 0x7) != FS_FILE` → `return -1` at elf.c:1527-1531; (3) `error_code` initialized to -1 at elf.c:1508; (4) pmap_create failure sets `error_code = -1` at elf.c:1593; (5) vm_map_create failure sets `error_code = -1` at elf.c:1614; (6) exec_setup_stack failure at elf.c:1740-1743 discards the function's negative return (`if (exec_setup_stack(.…

### SVR3/SVR4: ulimit(2) dispatched to sys_dup2
- **Location:** `sys/exec/perso/perso_svr3.c:46`  ·  abi-correctness  ·  verdict **CONFIRMED**
- **Subsystem:** Linux/SysV personalities
- **Defect:** The SVR3 (perso_svr3.c:46) and SVR4 (perso_svr4.c:48) syscall tables map SVR3_SYS_ulimit / SVR4_SYS_ulimit to &sys_dup2. ulimit(2) (query/set file-size limit, args: cmd, newlimit) and dup2(2) (args: oldfd, newfd) have entirely unrelated semantics. A binary invoking ulimit is silently routed into dup2 with its arguments reinterpreted as file descriptors.
- **Failure scenario:** An SVR3/SVR4 binary calls ulimit(UL_GETFSIZE) or ulimit(UL_SETFSIZE, n). The kernel instead executes dup2(cmd, newlimit): it may duplicate over / close an unrelated file descriptor (e.g. dup2(2, n) clobbers fd n), returns a bogus 'limit', and corrupts the process's fd table instead of reporting a resource limit.
- **Verifier:** perso_svr3.c:46 and perso_svr4.c:48 really do map slot 63 (SVR3_SYS_ulimit / SVR4_SYS_ulimit per svr3_syscalls.h:38 and svr4_syscalls.h:40) to &sys_dup2, while the adjacent names table (perso_svr3.c:88 "ulimit") and fmt table (perso_svr3.c:125, {2,{ARG_INT,ARG_INT}}) show the slot is meant to be ulimit — a clear copy-paste from the Linux i386 table where syscall 63 is dup2. It is not a stand-in for a missing handler: sys_ulimit(int,long) exists as an -ENOSYS stub at sys/exec/perso/compat.c:376 and is declared in sys/include/sys/syscall_impl.h:42, which perso_svr3.c already includes. The path i…

### SYN_SENT accepts SYN|ACK without validating the acknowledgement number
- **Location:** `sys/net/tcp.c:488`  ·  logic  ·  verdict **CONFIRMED**
- **Subsystem:** TCP/IPv4
- **Defect:** tcp_in_syn_sent() transitions to ESTABLISHED whenever a segment matching the 4-tuple carries SYN|ACK, checking only `(int32_t)(ack - p->snd_una) > 0` before pruning. RFC 793 requires that in SYN-SENT an incoming ACK be validated (ack must equal snd_nxt, i.e. acknowledge our SYN; ack <= iss or ack > snd_nxt must be rejected with a RST). Here, if ack does not advance snd_una the code STILL sets p->state = TCP_ESTABLISHED and sends an ACK — but never prunes the SYN from the unacked queue and leaves snd_una stale. Combined with the absence of any check that ack == snd_nxt, a blind/off-path SYN|ACK that merely guesses the 4-tuple (or a stale/duplicate SYN|ACK) can force the connection to 'establish' with an incorrect send state.
- **Failure scenario:** A client is in SYN_SENT (SYN queued, snd_una=iss, snd_nxt=iss+1). A segment arrives with SYN|ACK and an ack value that is <= snd_una (e.g. a stale duplicate, or an attacker who guessed sport/dport). The `ack - snd_una > 0` test fails so the SYN is never pruned, yet the code sets state=ESTABLISHED and sends an ACK. The connection is now 'up' with the SYN still sitting at unacked_head: it is RTO-retransmitted, snd_una is wrong, and any subsequent tcp_send flow-control math (snd_nxt - snd_una) is skewed. On real networks this permits blind connection establishment / desync rather than the RFC-mandated RST.
- **Verifier:** Read sys/net/tcp.c: tcp_in_syn_sent (lines 482-500) transitions to TCP_ESTABLISHED unconditionally on any SYN|ACK matching the 4-tuple. The only guard, `(int32_t)(ack - p->snd_una) > 0`, gates ONLY the prune/snd_una advance (lines 492-495); state=ESTABLISHED, tcp_send_ctl(ACK), and sched_wakeup all execute outside it (lines 496-498). There is no RFC-793 ack validation (no `ack <= iss` / `ack > snd_nxt` → RST, no `ack == snd_nxt` check). State setup is exactly as claimed: tcp_connect_start sets snd_una=iss, snd_nxt=iss, then tcp_xmit_queue(TCP_SYN) queues the SYN at unacked_head (seq=iss) and b…

### pmap_fork treats a large-page (PTE_PS) user PDE as a page table
- **Location:** `sys/arch/i386/pmap.c:664`  ·  logic  ·  verdict **CONFIRMED**
- **Subsystem:** arch/i386 memory
- **Defect:** The pmap_fork() PDE loop (line 663) only tests PTE_P before doing `src_pt_phys = src_pd[pdi] & ~0xFFF` and dereferencing (src_pt_phys+0xC0000000) as a 1024-entry page table. It never checks PTE_PS. pmap_enter_large() can install a 4 MiB user PDE (it sets PTE_U for va < 0xC0000000). For such a PDE the '&~0xFFF' value is the 4 MiB frame plus stray attribute bits, and the loop would interpret the mapped 4 MiB page's data bytes as PTEs — calling pmm_get_page on garbage, vm_page_hold-ing random frames, and mangling refcounts. Currently latent: no in-tree caller of pmap_enter_large exists (verified by grep), so no user large pages are ever created. pmap_destroy() and pmap_copyin/out_other correctly special-case PTE_PS, making the fork omission an inconsistency waiting to bite if user large-page mappings are ever enabled.
- **Failure scenario:** If a future personality/mmap path uses pmap_enter_large for a user VA, then fork() of that process makes pmap_fork walk the 4 MiB page contents as a page table: bogus pmm_get_page()/vm_page_hold() on attacker-influenced page data corrupts vm_page refcounts and installs junk child PTEs → use-after-free / arbitrary-frame aliasing.
- **Verifier:** Read sys/arch/i386/pmap.c directly. pmap_fork's PDE loop (lines 663-673) tests only PTE_P, then does `src_pt_phys = src_pd[pdi] & ~0xFFF` and walks (src_pt_phys+0xC0000000) as 1024 PTEs (lines 691-711), calling pmm_get_page/vm_page_hold/pv_insert on each word — no PTE_PS check exists, so a 4 MiB user PDE's page data would be interpreted as PTEs exactly as claimed. pmap_enter_large (line 1076) genuinely can create such a PDE: line 1142 sets PTE_U for va < 0xC0000000 and line 1151 writes pa|PTE_P|PTE_PS into the PD (line 1090 only requires the pmap to be CR3-active, which a self-mapping process …

### Signal handler entered with 16-byte-aligned ESP (i386 SysV ABI wants ESP%16==12)
- **Location:** `sys/arch/i386/signal.c:405`  ·  abi-correctness  ·  verdict **CONFIRMED**
- **Subsystem:** arch/i386 traps & context
- **Defect:** sendsig() builds the signal frame with `esp -= sizeof(frame); esp &= ~0xFUL;` (both legacy path line 405 and SA_SIGINFO path line 353) and delivers control with regs->useresp = esp, where [esp] is the trampoline return address. That makes ESP ≡ 0 (mod 16) at the handler's first instruction. The i386 System V ABI (as implemented by GCC/Linux) requires the stack to be 16-byte aligned at a `call` site, i.e. ESP ≡ 12 (mod 16) at callee entry after the return address is pushed. Linux's setup_frame deliberately computes `sp = ((sp+4) & -16) - 4` to satisfy this; substrate omits the `-4`, leaving the frame misaligned by 4 bytes. A signal handler (or any function it calls) that GCC compiled to place a 16-byte-aligned SSE local and access it with movaps/movdqa will fault.
- **Failure scenario:** A program installs a handler containing/inlining SSE code that uses `movaps` on a stack-resident 16-byte-aligned local (typical GCC output at -O2 with SSE). Kernel delivers the signal with ESP%16==0; the compiler assumed ESP%16==12, so the aligned local is actually 4-byte-misaligned -> movaps raises #GP -> SIGSEGV inside the handler.
- **Verifier:** sys/arch/i386/signal.c really does `esp -= sizeof(frame); esp &= ~0xFUL;` in both the legacy path (lines 402-405) and SA_SIGINFO path (lines 350-353), then sets regs->useresp = esp with the trampoline retaddr at frame offset 0 (signal_arch.h structs sigframe/siginfo_frame both start with uint32_t retaddr). The handler therefore starts with ESP ≡ 0 (mod 16), while GCC's i386 ABI expects ESP ≡ 12 (mod 16) at entry (16-aligned before the implied call). The path is reachable for every native-personality binary: sys/kern/signal.c:1837 dispatches p->sendsig and perso_native.c:626 sets .sendsig = sen…

### arch_fork_with_stack / arch_clone_thread return bare -1 (wrong errno contract)
- **Location:** `sys/arch/i386/syscall.c:606`  ·  error-handling  ·  verdict **CONFIRMED**
- **Subsystem:** arch/i386 traps & context
- **Defect:** arch_fork_with_stack() (line 606) and arch_clone_thread() (line 628) return a bare `-1` on the `!current_thread || !current_thread->syscall_regs` guard. sys_fork() propagates that -1 straight to userspace. Per the kernel's errno contract (libc negates the kernel return into errno), a bare -1 surfaces to userspace as EPERM rather than a meaningful error such as -EINVAL/-EFAULT. This is the same bare-`-1`-as--errno defect flagged elsewhere in the tree. The guarded condition is a should-not-happen internal case, so severity is low, but the return value is contractually wrong.
- **Failure scenario:** If sys_fork() is ever entered without a valid syscall_regs frame, the caller receives -1 -> errno=EPERM ('Operation not permitted') instead of a fork-appropriate error, misleading userspace error handling.
- **Verifier:** Read sys/arch/i386/syscall.c directly. Lines 606 and 628 do return bare -1 on the `!current_thread || !current_thread->syscall_regs` guards, and sys_fork() (lines 637-639) returns arch_fork_with_stack(NULL) unmodified, so the -1 would surface to userspace as errno EPERM under the kernel's negative-errno contract (the convention is confirmed by the pervasive `return -EINVAL/-EBADF` style in kern/syscall.c and is an acknowledged tree-wide bare-`-1` cleanup class in this project). However, the guard is effectively dead code today: syscall_handler() unconditionally sets current_thread->syscall_reg…

### rename '..' fixup trusts dot->rec_len → OOB access in dir block buffer
- **Location:** `sys/fs/ext2/ext2.c:3235`  ·  memory-safety  ·  verdict **CONFIRMED**
- **Subsystem:** ext2 filesystem
- **Defect:** In ext2_rename, when moving a directory across parents, the '..' update computes `dotdot = (ext2_dirent_t *)(old_node_ctx->block_buf + dot->rec_len)` using the on-disk rec_len of the first ('.') entry without validating it against block_size. dot->rec_len is a uint16 read straight from disk; a corrupt directory block with dot->rec_len near 0xFFFF places dotdot far past the block_size buffer, and the subsequent reads of dotdot->name_len/name[] (and, if they happen to match '..', the write dotdot->inode + ext2_write_block) touch memory beyond block_buf.
- **Failure scenario:** Rename a directory (to a different parent) whose first data block has a corrupt '.' entry with rec_len=0xFFFA on a 1024/4096-byte-block fs. old_node_ctx->block_buf + 0xFFFA points past the allocated block buffer; dereferencing dotdot reads OOB, and a coincidental '..' name match writes dotdot->inode OOB then flushes it to disk.
- **Verifier:** sys/fs/ext2/ext2.c lines 3233-3239: after ext2_read_block fills old_node_ctx->block_buf, the code computes `dotdot = (ext2_dirent_t *)(old_node_ctx->block_buf + dot->rec_len)` with dot->rec_len taken raw from the on-disk first dirent — no `rec_len < 8` / `rec_len > fs->block_size` check, unlike every other dirent walker in the file (lines 1703, 1756, 1989, 2963, 3312 all validate exactly this). rec_len is uint16, so a corrupt value near 0xFFFF indexes up to ~64 KiB past the kmalloc(block_size) buffer; line 3236 unconditionally reads dotdot->name_len/name[] OOB, and a coincidental "..\0x02" byt…

### UDF error paths leak kmalloc'd buffers
- **Location:** `sys/fs/udf/udf_write.c:1181`  ·  resource-leak  ·  verdict **CONFIRMED**
- **Subsystem:** minix/udf/9p/sysv/procfs
- **Defect:** Several UDF error paths return without freeing an already-allocated buffer. udf_truncate_file kmallocs sector_buf at line 1169 and returns -1 at line 1181 (inline-too-large), line 1220 (!zero_buf), line 1263 (new_block==0), line 1355 (!zero_buf) and line 1393 (new_block==0) without kfree(sector_buf). udf_read_space_bitmap (udf_write.c line 57) kmallocs sector_buf (2048*4) and returns -1 at line 64 (`sectors > 4`) without freeing it. Each triggering event leaks a kernel buffer.
- **Failure scenario:** Mount a UDF volume whose space bitmap spans more than 4 sectors, or ftruncate-extend an inline UDF file beyond one sector: the corresponding early return leaks the sector buffer; repeated operations exhaust kernel heap.
- **Verifier:** All six cited sites verified by reading sys/fs/udf/udf_write.c. udf_truncate_file kmallocs sector_buf at line 1169 and: (1) line 1181 returns -1 on the inline-too-large check with no kfree — reachable by a plain ftruncate extending an inline file, repeatable 2 KiB leak per call; (2) line 1220 returns -1 on !zero_buf without freeing sector_buf; (3) line 1263 (short-AD new_block==0) frees zero_buf but not sector_buf; (4) line 1355 (long-AD !zero_buf) leaks sector_buf; (5) line 1393 (long-AD new_block==0) frees zero_buf only. The adjacent error path at lines 1290-1295 frees BOTH buffers, proving …

### minix_finddir: kmalloc result used without NULL check
- **Location:** `sys/fs/minix/minix.c:905`  ·  error-handling  ·  verdict **CONFIRMED**
- **Subsystem:** minix/udf/9p/sysv/procfs
- **Defect:** minix_finddir does `result = (fs_node_t *)kmalloc(sizeof(fs_node_t)); memset(result, 0, sizeof(fs_node_t));` with no NULL check between the allocation and the memset (lines 905-906). Under kernel heap exhaustion kmalloc returns NULL and the memset dereferences a NULL pointer.
- **Failure scenario:** Directory lookup on a minix filesystem while the kernel heap is exhausted → kmalloc returns NULL → memset(NULL,...) → NULL-pointer write/panic.
- **Verifier:** sys/fs/minix/minix.c:905-906 reads exactly as claimed: `result = (fs_node_t *)kmalloc(sizeof(fs_node_t)); memset(result, 0, sizeof(fs_node_t));` with no NULL check, then passes result to minix_read_inode. kmalloc (sys/vm/vm_kmem.c:92-129) allocates via uma_zalloc(..., M_NOWAIT) and demonstrably returns NULL on exhaustion (it even logs "kmalloc: uma_zalloc failed" and returns the NULL result), so the NULL path is real. minix_finddir is the VFS finddir op, reachable from any path lookup on a mounted minix fs, with no guard elsewhere. The adjacent minix_mount (lines 922-924) does check its kmallo…

### fork error paths and proc_destroy leak the chroot root_node vnode reference
- **Location:** `sys/pm/process.c:627`  ·  resource-leak  ·  verdict **CONFIRMED**
- **Subsystem:** power/pm subsystem
- **Defect:** proc_create() takes a vnode reference on the inherited root when chrooted (open_fs(proc->root_node) at line 438). None of proc_fork_common's failure paths releases it: the pmap_fork/vm_map_fork/ldt_clone failures go straight to proc_destroy() (which never touches root_node), and the sched_fork_thread failure cleanup (lines 613-644) closes cwd_node at 628 but not root_node. Only the successful-exit path (proc_exit line 1694) closes it.
- **Failure scenario:** A chrooted daemon (e.g. anything under /perso/<os> or an ftpd) fork()s under memory pressure; each failed fork leaks one reference on the chroot directory vnode. The vnode (and its filesystem's mount) can never be released/unmounted, and repeated failures pin ext2 node cache entries permanently.
- **Verifier:** proc_create() (sys/pm/process.c:436-439) does open_fs(proc->root_node,1,0) whenever the inherited root != fs_root, i.e. every fork by a chrooted parent takes a vnode reference in the child. proc_destroy() (process.c:387-400) is only fpu_forget_process + proc_unlink_locked + proc_storage_free — it never closes root_node. Grepping root_node in process.c shows the only close is in proc_exit (lines 1694-1696), which a fork-failure child never reaches. All four proc_fork_common failure legs — pmap_fork (490), vm_map_fork (502), ldt_clone_process (523), sched_fork_thread (643) — end in proc_destroy(…

### sys_setpgid leaks a freshly allocated session when pgrp_alloc fails
- **Location:** `sys/pm/pgrp.c:397`  ·  resource-leak  ·  verdict **CONFIRMED**
- **Subsystem:** power/pm subsystem
- **Defect:** In the create-new-group path of sys_setpgid, when the caller has no session, caller_sess = session_alloc(target) is allocated (line 393); if the subsequent pgrp_alloc(target, caller_sess) fails, the function returns -1 without session_free(caller_sess), leaking the session struct. (Contrast sys_setsid, which does free the session on the same failure.)
- **Failure scenario:** Under kmalloc exhaustion, session_alloc succeeds but pgrp_alloc's kmalloc fails; the just-allocated struct session is orphaned — no pointer to it remains anywhere. Repeated attempts leak kernel heap.
- **Verifier:** The leak is real and reachable. In /home/segin/substrate/sys/pm/pgrp.c, sys_setpgid's create-new-group path reads: `if (!caller_sess) { caller_sess = session_alloc(target); if (!caller_sess) return -1; } new_pgrp = pgrp_alloc(target, caller_sess); if (!new_pgrp) return -1;` (lines 391-397). On that last `return -1` nothing frees the session. Three facts make this a genuine leak: (1) session_alloc (lines 45-61) does NOT register the session anywhere — the "Add to global session list" comment at lines 57-58 is a no-op, so the local `caller_sess` is the only pointer; (2) session_find (lines 425-4…

### Grandchildren's fault/context-switch counters are double-counted in wait4
- **Location:** `sys/pm/wait.c:192`  ·  logic  ·  verdict **CONFIRMED**
- **Subsystem:** power/pm subsystem
- **Defect:** rusage_finalize() (rusage.c lines 207-216) already folds target->rusage_children into target->rusage at exit (ru_minflt, ru_majflt, ru_nvcsw, ru_nivcsw, ...). kern_wait4 then accumulates `target->rusage.ru_minflt + target->rusage_children.ru_minflt` (and likewise majflt/nvcsw/nivcsw, lines 192-195) into the parent's rusage_children — adding the grandchildren's counts a second time.
- **Failure scenario:** P forks C, C forks and reaps G (G has 100 minor faults). C exits: rusage_finalize gives C->rusage.ru_minflt = C_own + 100. P's wait4 then adds C->rusage.ru_minflt + C->rusage_children.ru_minflt = C_own + 200 — getrusage(RUSAGE_CHILDREN) in P over-reports every grandchild's faults and context switches by 2x (compounding per generation).
- **Verifier:** The defect is real and I traced the full path. (1) proc_exit calls rusage_finalize(current_process) at sys/pm/process.c:1752, before the process becomes SZOMB/reapable. (2) rusage_finalize (sys/pm/rusage.c:187-216) folds p->rusage_children into p->rusage for the scalar counters — including `p->rusage.ru_minflt += p->rusage_children.ru_minflt;` (line 207), majflt (208), nvcsw (215), nivcsw (216) — and never zeroes p->rusage_children afterward (the only memsets of rusage_children are in rusage_init/rusage_copy_to_child, lines 232/249, which run at process creation, not at exit). (3) kern_wait4's…

### pgrp/session syscalls return bare -1 instead of -errno
- **Location:** `sys/pm/pgrp.c:342`  ·  error-handling  ·  verdict **CONFIRMED**
- **Subsystem:** process & signals
- **Defect:** sys_setpgid (returns -1 for the ESRCH/EINVAL/EPERM cases at lines 352,354,373,377,386,394,397,401), sys_getpgid (330,332), sys_getsid (278,283), and sys_setsid (238,243,248) all return a bare -1 on error. libc negates the kernel return into errno, so a bare -1 becomes errno=EPERM (1) regardless of the real error. This is the documented Substrate contract violation (kernel must return -errno). setpgid()'s 'not a child' and 'different session' paths, getpgid()/getsid() on a nonexistent pid, and setsid()'s ENOMEM allocation failures all surface to userspace as EPERM instead of ESRCH / EINVAL / ENOMEM.
- **Failure scenario:** A process calls getpgid(nonexistent_pid). kern returns -1; libc sets errno=EPERM. POSIX/callers expect ESRCH, so error handling that branches on ESRCH vs EPERM (job-control shells, test suites) misbehaves. Likewise setpgid on a non-child returns EPERM where ESRCH is required, and setsid() under memory exhaustion reports EPERM instead of ENOMEM.
- **Verifier:** Read sys/pm/pgrp.c directly: every cited site is a bare `return -1` with a comment naming the errno it should be (e.g. line 238 `return -1; /* EPERM - already a group leader */`, 243/248 ENOMEM in sys_setsid; 278/282 in sys_getsid; 328/330 in sys_getpgid; 352, 354, 372 `/* ESRCH - not a child */`, 376, 386, 394, 397, 401 in sys_setpgid). Notably sys_setlogin in the same file correctly returns -EFAULT/-EPERM, so the file itself demonstrates the intended -errno convention. The dispatch path offers no rescue: sys/exec/perso/perso_native.c lines 199-202 map SYS_SETSID/GETSID/SETPGID/GETPGID straig…

### Timeshare MLFQ level uses bitwise AND (& 39) instead of range clamp
- **Location:** `sys/kern/runqueue.c:56`  ·  logic  ·  verdict **CONFIRMED**
- **Subsystem:** scheduler & SMP
- **Defect:** runqueue_level_for_thread() maps a SCHED_TIMESHARE thread to a queue with `RQ_TIMESHARE_BASE + (t->priority & 39)`. 39 is 0b100111, so the AND masks OUT priority bits 3 and 4 (values 8 and 16) rather than clamping/modulo-ing into 0..39. Distinct timeshare priorities therefore collapse onto the same MLFQ level and the mapping is non-monotonic: priority 24 (0b11000) & 39 = 0, priority 8 & 39 = 0, priority 12 & 39 = 4, priority 20 & 39 = 4. The RT case one line above correctly uses `& 31` (a valid mask for 0..31), showing the intent was a range reduction; `& 39` does not achieve it. The timeshare priority ordering encoded in the runqueue is scrambled — a niced-down thread can land in the same or a better queue level than a high-priority one.
- **Failure scenario:** A timeshare thread with priority 24 and one with priority 0 both hash to level RQ_TIMESHARE_BASE+0; a priority-12 thread outranks a priority-8 thread's placement. Any consumer of runqueue ordering (runqueue_peek/pop, work stealing) picks by scrambled priority. Currently latent because normal scheduling is done by sched_yield()'s allthread scan and runqueues are only populated via the (unwired) affinity-migrate / periodic-balance paths; becomes an active priority inversion the moment the runqueue path is used for dispatch.
- **Verifier:** sys/kern/runqueue.c:56 really is `return RQ_TIMESHARE_BASE + (t->priority & 39);`. 39 = 0b100111 masks out priority bits 3 and 4, so 24&39=0, 8&39=0, 12&39=4, 20&39=4 — the mapping is non-monotonic and collapses distinct priorities, exactly as claimed; the RT case at line 50 uses a valid `& 31` mask, confirming the intent was range reduction that `& 39` does not achieve. Timeshare priorities genuinely span 0..39: sched_decay.c clamps to 39 (lines 36/86), so values with bits 3/4 set occur in normal decay/boost operation. The mask result never exceeds 39, so there is no out-of-bounds (RQ_TIMESHA…

### sched_yield walks allthread without tid_lock (KERN-06 gap) — SMP UAF
- **Location:** `sys/pm/sched.c:413`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** scheduler & SMP
- **Defect:** sched_yield() scans the global allthread list (from `start` through t->t_allthread_next) protected only by intr_disable(), not by tid_lock. The codebase's KERN-06 invariant (documented at pm/sched.c:102 and applied to sched_tick() line 648 and sched_wakeup_n() line 816) is that any allthread/tid_hash walk must hold thread_registry_lock() because sched_reap_thread() unlinks and kfrees a thread_t under tid_lock. On a uniprocessor this walk is safe (interrupts disabled = no concurrent reaper), but on real SMP another CPU running sched_reap_thread()/sched_reap_process_threads() can unlink+free a thread_t while this CPU dereferences it mid-scan. sched_yield cannot simply take tid_lock (it context-switches mid-function), so this is a structural gap the current UP-only reality masks.
- **Failure scenario:** Genuine multi-core: CPU0 is inside sched_yield()'s while(t) scan holding a pointer to thread X; CPU1 executes sys_thr_join()->sched_reap_thread(X) which unlinks and kfrees X. CPU0 then reads X->t_allthread_next / X->state off freed (possibly reused) memory -> corrupted pick / kernel fault. Currently unreachable because APs park in hlt (only 1 live core).
- **Verifier:** Verified in sys/pm/sched.c: sched_yield() (line 365) guards its thread-pick scan only with intr_disable() (line 377) and walks the global allthread list at 404-485 (t = t->t_allthread_next at 479, wrap to allthread at 481) without ever taking tid_lock/thread_registry_lock(). The KERN-06 comment at lines 102-114 explicitly documents that any allthread/tid_hash walk must hold the registry lock because sched_reap_thread() (865-896) unlinks under spinlock_acquire_irq(&tid_lock) (891-893) and immediately kfrees the thread_t (sched_storage_free, 895). The sibling walkers comply — sched_tick takes th…

### Preemption IPI assumes CPU index == LAPIC ID
- **Location:** `sys/kern/sched_ipi.c:26`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** scheduler & SMP
- **Defect:** sched_send_preempt_ipi() sends the reschedule IPI with `lapic_send_ipi((uint8_t)cpu_id, SCHED_IPI_VECTOR)`, treating the logical CPU index directly as the destination LAPIC ID (the comment acknowledges 'assume CPU ID == LAPIC ID' and notes a cpu_to_lapic mapping is missing). smp_discovery.c stores a real cpus[i].lapic_id per core, so on topologies where LAPIC IDs are not identity-mapped to the 0..N-1 enumeration order (non-contiguous or renumbered APIC IDs), sched_resched_cpu() delivers the preemption IPI to the wrong physical CPU or a nonexistent LAPIC: the intended CPU is never preempted and an unrelated CPU takes a spurious preempt.
- **Failure scenario:** Hyperthreaded/multi-socket topology where core index 1 has LAPIC ID 2. sched_resched_cpu(1) -> sched_send_preempt_ipi(1) -> IPI to LAPIC 2 (wrong core); the runnable higher-priority thread on core 1 is not preempted onto -> added latency/starvation. Latent while multi-CPU scheduling is not live.
- **Verifier:** sys/kern/sched_ipi.c:26 does exactly what the finding says: `lapic_send_ipi((uint8_t)cpu_id, SCHED_IPI_VECTOR)` passes the logical CPU index straight through, and the comment above it admits 'assume CPU ID == LAPIC ID'. lapic_send_ipi_ex (sys/arch/x86-common/lapic.c:463) writes that value into LAPIC_ICRHI << 24 in physical destination mode, so it must be a physical APIC ID. The identity assumption is not guaranteed by construction: sys/arch/i386/smp_discovery.c stores a real cpus[i].lapic_id from the MP table/MADT (lines 180-188, 460-465), boots APs via smp_boot_ap(cpus[i].lapic_id) (line 663)…

### turnstile_release clobbers priority boosts from other still-held locks
- **Location:** `sys/kern/turnstile.c:196`  ·  logic  ·  verdict **CONFIRMED**
- **Subsystem:** sync primitives
- **Defect:** turnstile_release unconditionally restores ts_owner->priority = ts_owner->base_priority when the released turnstile had any inherited priority. It does not consider boosts the owner still legitimately carries from OTHER contended locks it continues to hold (no per-thread aggregation; turnstile_get_inherited_priority exists but has no callers). Releasing any one contended lock therefore drops the owner all the way to base priority even while a higher-priority waiter is still blocked on another lock it holds — reintroducing the priority inversion that turnstiles exist to prevent. Additionally, ts_inherited_prio is never recomputed when a boosted waiter leaves the queue (signal/timeout), so stale boosts persist until release.
- **Failure scenario:** Timeshare thread T (base prio 20) holds lockmgr locks L1 and L2. High-priority thread H (prio 5) blocks on L2 -> T boosted to 5. Low-priority waiter blocks on L1 creating L1's turnstile. T releases L1 first: turnstile_release(L1) sets T->priority = 20 while H is still blocked on L2. Mid-priority CPU-bound threads (prio 10) now preempt/starve T indefinitely -> H is delayed unboundedly: classic unbounded priority inversion.
- **Verifier:** The code defect is real exactly as described: sys/kern/turnstile.c:196-197 does `ts->ts_owner->priority = ts->ts_owner->base_priority` on release of any one turnstile with a nonzero ts_inherited_prio, with no aggregation across other turnstiles the same thread still owns; turnstile_get_inherited_priority (lines 209-230), which would provide that aggregation, has no callers outside the ksyms symbol tables; and there is no turnstile_remove_thread, so ts_inherited_prio is never recomputed when a signalled/timed-out waiter leaves (the file's own comment at lines 174-175 concedes waiters are never …

### turnstile global lock taken without preempt_disable
- **Location:** `sys/kern/turnstile.c:43`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** sync primitives
- **Defect:** ts_lock() is a raw test_and_set spin loop that raises neither preempt_count nor IRQ masking — exactly the pattern the sleepq.c KERN-01 comment (sleepq.c:79-86) documents as having caused intermittent hangs before the sleepq bucket locks were fixed. Today all turnstile_block/turnstile_release calls happen under lockmgr's lk_interlock (which disables preemption), masking the bug; but turnstile_get_inherited_priority() takes ts_lock with no such protection and is an exported kernel symbol (ksyms tables). Any caller of it (or any future turnstile call site not under a spinlock) can be preempted while holding turnstile_lock; a lockmgr release path then spins on ts_lock with preemption disabled, so on a single CPU the holder can never run again — a hard livelock/deadlock.
- **Failure scenario:** A future/module caller invokes turnstile_get_inherited_priority(t) from ordinary process context; the timer tick preempts it between ts_lock() and ts_unlock(). The newly scheduled thread performs lockmgr(LK_RELEASE) on any contended lock: spinlock_acquire(lk_interlock) disables preemption, then turnstile_release spins forever in ts_lock() — CPU locked up, holder unrunnable.
- **Verifier:** Every factual claim verifies against the code. ts_lock() (sys/kern/turnstile.c:43-48) is a raw __sync_lock_test_and_set spin loop with no preempt_disable() and no IRQ masking, violating the documented contract in sys/include/sys/preempt.h:9-11 ("every spinlock acquire raises preempt_count; the timer-IRQ path only preempts when it is 0"). This is the exact pattern sleepq.c's sq_lock() KERN-01 comment documents as the cause of the historical intermittent pipe/mutex/pty hangs before those buckets got cli+preempt_disable. All in-tree turnstile_block (lockmgr.c:90,128) and turnstile_release (lockmg…

### shmat with explicit address destroys existing mapping before it can fail
- **Location:** `sys/kern/ipc_shm.c:280`  ·  error-handling  ·  verdict **CONFIRMED**
- **Subsystem:** syscall layer & IPC
- **Defect:** In kern_shmat(), when the caller passes a non-zero shmaddr, the code calls vm_map_remove(map, virt, virt+length) to clear whatever is already mapped there (SHM_REMAP-ish behaviour) BEFORE the attachment can actually be established. The subsequent steps — vm_object_allocate(), vm_pager_allocate(), vm_map_insert(), and shm_attach_add() — can each still fail (ENOMEM under memory pressure). On any of those failures the function returns an error, but the user's previously-mapped region at [virt, virt+length) has already been torn down and is not restored. This is non-atomic remap: a failed shmat is documented/expected to leave the address space unchanged, but here it can leave a hole. (The shmaddr==0 path is unaffected — it uses vm_map_find_space into free space and destroys nothing.)
- **Failure scenario:** A process calls shmat(shmid, addr, 0) with addr overlapping a live region of its own address space (e.g. heap or a data mapping). kern_shmat runs vm_map_remove over [addr, addr+seg_len), unmapping that region. Then vm_object_allocate() (or vm_pager_allocate / vm_map_insert) returns NULL/ENOMEM because the system is low on memory. shmat returns -ENOMEM, but the original region is now gone; the next access to the old data faults and the process takes an unexpected SIGSEGV even though it believes shmat merely failed.
- **Verifier:** sys/kern/ipc_shm.c kern_shmat (lines 237-314) does exactly what the finding claims. For non-zero shmaddr, line 280 calls vm_map_remove(map, virt, virt+length) — commented "Clear anything already mapped there (SHM_REMAP-ish behaviour)" — BEFORE any of the attachment machinery runs. After that point there are four independently reachable failure returns that leave the destroyed region unrestored: vm_object_allocate() NULL at line 286 (alloc_object() can fail, verified sys/vm/vm_object.c:28-31), vm_pager_allocate() NULL at line 291, vm_map_insert() failure at line 300, and shm_attach_add() failur…

### psf2_parse: unsigned underflow of (size - headersize) bypasses glyph-data bounds check
- **Location:** `sys/drivers/video/psf.c:80`  ·  integer-overflow  ·  verdict **CONFIRMED**
- **Subsystem:** video & console
- **Defect:** psf2_parse validates only that hdr->headersize >= sizeof(struct psf2_header) (psf.c:68) but never that headersize <= size. headersize is an attacker-controlled uint32 from the font header. The bounds check `if (glyph_data_size > size - hdr->headersize)` (psf.c:80) computes size - headersize in unsigned (size_t) arithmetic; when headersize > size this underflows to a near-SIZE_MAX value, so the comparison always passes. out->glyphs is then set to data + headersize (psf.c:98), a pointer far past the buffer, and out->unicode_table math (psf.c:102) is likewise poisoned. Any consumer that reads glyph bytes performs an out-of-bounds read. (Currently the PSF2 parser is exported but not yet wired to a live load-from-userspace/disk path, so this is latent; it becomes directly exploitable the moment a setfont/KDFONTOP-style path calls it on untrusted data.)
- **Failure scenario:** A 40-byte PSF2 blob with valid magic, version 0, numglyph=1, bytesperglyph=1, width=1, height=1, and headersize=0x40000000. sizeof check passes (headersize>=32); glyph_data_size=1; size(40)-headersize wraps to ~0xC0000028; 1 > that is false so the check passes; out->glyphs = data + 0x40000000. font_get_glyph()/rendering reads 1GB past the allocation -> OOB read / fault.
- **Verifier:** The defect is real in the code as written. sys/drivers/video/psf.c:68 checks only `hdr->headersize < sizeof(struct psf2_header)`; there is no `headersize <= size` check anywhere in psf2_parse. Line 80 (`if (glyph_data_size > size - hdr->headersize)`) does the subtraction in size_t; the overflow guard at lines 76-78 only covers the numglyph*bytesperglyph multiply, not this subtraction. I traced the claimed 40-byte blob (headersize=0x40000000, numglyph=bytesperglyph=width=height=1) through every check: line 68 passes (0x40000000 >= 32), line 80's RHS wraps to ~0xC0000028 so 1 > that is false, li…

### uma_zfree double-free guard is a non-atomic check-then-set (TOCTOU)
- **Location:** `sys/vm/uma_core.c:1000`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** vm allocators
- **Defect:** The double-free tripwire in uma_zfree() reads *df_mark, compares against UMA_ITEM_ALLOCED, then stores UMA_ITEM_BUCKETED (lines 998-1006) with no lock, no atomic, and interrupts still enabled. Two CPUs (or a thread plus an IRQ-context kfree on another CPU) freeing the same item concurrently can both observe ALLOCED and both pass the guard — the precise bug class the guard exists to catch. The item is then pushed into two per-CPU free buckets and will be handed out twice by subsequent uma_zalloc calls.
- **Failure scenario:** A driver bug frees the same mbuf-sized buffer from the netstack RX IRQ path on CPU 0 and from a process-context teardown on CPU 1 within the race window: both pass the ALLOCED check, the item lands in both CPUs' free buckets, two later kmalloc() callers receive the same memory -> undetected double allocation and heap corruption, with the diagnostic panic that should have fired never triggering.
- **Verifier:** The claim is accurate as written. In sys/vm/uma_core.c, uma_zfree() (lines 994–1006) performs the double-free guard as a plain non-atomic read-compare-store on the slab marker byte: `uint8_t *df_mark = uma_item_marker(...); if (*df_mark != UMA_ITEM_ALLOCED) panic(...); *df_mark = UMA_ITEM_BUCKETED;` — with no lock held and interrupts enabled. The function's intr_disable() critical section does not start until line 1022, after the marker update, dtor, and poison calls. uma_item_marker() (976–992) only takes uma_slab_lock transiently inside uma_find_slab for the hash lookup and returns a raw poi…

### kmem_get_stats/kmem_get_snapshot take kmem_stats_lock without disabling IRQs
- **Location:** `sys/vm/vm_kmem.c:300`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** vm allocators
- **Defect:** kmalloc/kfree deliberately use spinlock_acquire_irq on kmem_stats_lock because kfree runs from IRQ context (netstack RX — documented in the file at lines 96-99). But kmem_get_stats (line 300) and kmem_get_snapshot (line 312) take the same lock with plain spinlock_acquire, which disables only preemption, not interrupts (sys/kern/spinlock.c). An IRQ that lands on the same CPU while the snapshot holds the lock and calls kfree() hits spinlock_acquire's same-CPU-held check and panics ('Deadlock: Spinlock already held by current CPU'). Currently latent — no in-tree caller of either function was found — but both are exported via the ksyms tables, so any future /proc consumer or module trips it under network load.
- **Failure scenario:** A future /proc/memtrack-style reader calls kmem_get_snapshot during heavy network RX; a NIC interrupt arrives on the same CPU between acquire and release, the IRQ handler's kfree calls spinlock_acquire_irq on the already-held kmem_stats_lock -> immediate deadlock panic.
- **Verifier:** Verified every element in the actual code. sys/vm/vm_kmem.c: kmalloc/kfree take kmem_stats_lock with spinlock_acquire_irq at all 7 sites (lines 101, 111, 136, 148, 157, 179, 224), with the comment at 96-99 explicitly stating the IRQ-save form is needed because netstack RX calls kfree() from IRQ context. kmem_get_stats (line 300) and kmem_get_snapshot (line 312) take the same lock with plain spinlock_acquire, which per sys/kern/spinlock.c:14-39 only does preempt_disable() and never disables interrupts; its same-CPU-held check panics with "Deadlock: Spinlock already held by current CPU" (line 31…

### vm_phys_mark_used reserves pages without setting PG_PMM_ALLOC
- **Location:** `sys/vm/phys_mem.c:513`  ·  logic  ·  verdict **CONFIRMED**
- **Subsystem:** vm allocators
- **Defect:** vm_phys_mark_used() dequeues and splits a free buddy block, then marks the target page used with only 'flags &= ~PG_FREE; order=0; ref_count=1' (lines 513-515) — it never sets PG_PMM_ALLOC, the flag the allocator's own tripwires key on. Every normally-allocated page gets PG_PMM_ALLOC in vm_phys_prepare_allocated_block, and vm_phys_free_page panics ('free of unallocated page', line 338) when it is absent. So any page reserved via mark_used (kernel image, watermark, boot regions — see sys/arch/i386/pmm.c) that is ever legitimately released panics the kernel as a false-positive double-free. Additionally, the mid-split 'if (!right) break;' (line 503) abandons a multi-page block off all free lists while decrementing vm_phys_free_count by only 1, silently leaking the remainder. Latent today because boot-reserved pages are never freed, but it is an armed trap for any future release of a boot region.
- **Failure scenario:** Future code frees a formerly boot-reserved page (e.g. reclaiming multiboot module memory after module load) via pmm_free_block -> vm_phys_free_page sees PG_PMM_ALLOC clear -> panic('vm_phys: free of unallocated page') on a perfectly valid free.
- **Verifier:** Read sys/vm/phys_mem.c directly. vm_phys_mark_used() (lines 477-520) reserves a page with only 'head->flags &= ~PG_FREE; head->order = 0; head->ref_count = 1' (513-515) and never sets PG_PMM_ALLOC, while every normal allocation goes through vm_phys_prepare_allocated_block() which sets 'p->flags |= PG_PMM_ALLOC' (line 70). vm_phys_free_page() unconditionally panics ('vm_phys: free of unallocated page', lines 333-338) when ref_count<=1 and PG_PMM_ALLOC is clear — no exemption for reserved pages exists, and vm_page.c:604-606 explicitly documents PG_PMM_ALLOC as the invariant the tripwire relies o…

### vm_object bootstrap pool allocation counter is unsynchronized
- **Location:** `sys/vm/vm_object.c:16`  ·  concurrency  ·  verdict **CONFIRMED**
- **Subsystem:** vm allocators
- **Defect:** alloc_object() does 'return &bootstrap_objects[next_bootstrap_object++]' with no lock or atomic. vm_object_allocate is callable at runtime (mmap, fork shadow creation) as well as during boot; until the 32-entry bootstrap pool is exhausted, two CPUs allocating objects concurrently can read the same index and both receive the same vm_object_t, which each then fully reinitializes (ref_count=1, pages=NULL, magic).
- **Failure scenario:** Early multi-CPU userland (two processes mmap simultaneously shortly after boot, before 32 objects have been created): both maps end up sharing one aliased vm_object; the second initialization wipes the first mapping's page list and ref_count -> lost resident pages, ref-count underflow panic in vm_object_deallocate, or cross-process page aliasing.
- **Verifier:** sys/vm/vm_object.c:15-21 is exactly as claimed: alloc_object() returns &bootstrap_objects[next_bootstrap_object++] on a plain static int with no lock or atomic, and the bootstrap branch precedes the kmalloc_ready branch, so the pool is the live allocation path until all 32 slots are consumed. Freed bootstrap objects are never recycled (lines 142-144 only mark them VM_OBJ_TYPE_DEAD), so the racy window is precisely the first 32 allocations. Reachability holds: no vm_object is allocated during single-threaded boot — every caller (vm_syscalls.c:166/:452 mmap, elf.c:736 per-PT_LOAD exec, ipc_shm.c…

### sys_msync returns bare -1 instead of -errno
- **Location:** `sys/vm/vm_syscalls.c:694`  ·  error-handling  ·  verdict **CONFIRMED**
- **Subsystem:** vm map & fault
- **Defect:** sys_msync's early guard `if (!current_process || !current_process->vm_map) return -1;` returns a bare -1. Per the codebase contract the libc wrapper negates the kernel return into errno, so -1 becomes EPERM rather than a meaningful error (EINVAL/EFAULT). Every other errno path in this file was converted to -Exxx; this one was missed. Low impact because a null current_process during a syscall is essentially unreachable, but it is an inconsistent errno contract.
- **Failure scenario:** If reached (no vm_map), userspace msync sees errno=EPERM instead of EINVAL, misleading callers that branch on the errno.
- **Verifier:** sys/vm/vm_syscalls.c:694 reads `if (!current_process || !current_process->vm_map) return -1;` — a bare -1. The libc wrapper (lib/c/src/posix_extra.c:451-455) does `errno = (int)-r` on negative returns, so -1 surfaces as errno=1=EPERM, exactly as claimed. The inconsistency claim is also verified: the same function returns a proper -EBUSY at line 716 for the MS_INVALIDATE/VME_WIRED path, so this file follows the -Exxx convention and this guard was missed. Reachability is effectively nil: sys_msync is dispatched only from personality syscall tables (perso_native.c:158, perso_netbsd.c:452, FreeBSD…

### sys_mprotect end computation can wrap 32-bit, silently protecting nothing
- **Location:** `sys/exec/perso/compat.c:244`  ·  integer-overflow  ·  verdict **CONFIRMED**
- **Subsystem:** vm map & fault
- **Defect:** sys_mprotect computes `end = (start + len + 0xFFF) & ~0xFFF` with 32-bit uintptr_t and no overflow guard on len (unlike sys_mmap/sys_munmap which use vm_round_page_size). A large len makes start+len+0xFFF wrap to a small value; the subsequent `end > 0xC0000000` check then passes, and vm_map_protect is called with end < start, whose loops (`if cur->start >= end break`) immediately terminate — so the call returns 0 (success) having changed no protections. A program that relies on mprotect to enforce a permission change would believe it succeeded while the region's protection is unchanged.
- **Failure scenario:** mprotect(addr, (size_t)-0x100, PROT_READ) with addr in user space: end wraps below addr, vm_map_protect no-ops, mprotect returns 0. Caller assumes the range is now read-only but it is not (e.g. a JIT that mprotects a code page to R-X still has it writable).
- **Verifier:** The defect is real exactly as described. sys_mprotect at /home/segin/substrate/sys/exec/perso/compat.c:232-248 computes `uintptr_t end = (start + len + 0xFFF) & ~0xFFF;` (line 244) with no overflow guard on len; the only range check is `if (start >= 0xC0000000 || end > 0xC0000000) return -EINVAL;` (line 247), which a wrapped end passes. Arithmetic check of the claimed scenario on 32-bit: start=0x08048000 (page-aligned, passes line 236), len=(size_t)-0x100=0xFFFFFF00 → start+len wraps to 0x08047F00, +0xFFF=0x08048EFF, &~0xFFF=0x08048000, so end==start; both are < 0xC0000000, so the check passes…

### pmap_enter on a non-current pmap switches CR3 but is not preemption-safe
- **Location:** `sys/arch/i386/pmap.c:838`  ·  concurrency  ·  verdict **PLAUSIBLE**
- **Subsystem:** arch/i386 memory
- **Defect:** pmap_enter()/pmap_enter_batch() call pmap_activate_for_update() (line 784), which loads CR3 with the target pmap and sets curpmap, then mutate page tables through the recursive self-map (V_PD/V_PT) that now resolves to the target. Nothing disables preemption or interrupts across this window. If the thread is preempted after the CR3 switch, the scheduler (sys/pm/sched.c:358) calls pmap_activate(next->proc->pmap) and later, on resume, pmap_activate(this thread's own process pmap) — NOT the temporary target. Execution then continues writing PTEs via V_PD, which now points at the wrong (the running process's) address space. ioremap() (sys/kern/ioremap.c:76) calls pmap_enter(pmap_kernel(),...) from driver-probe/process context, the classic non-current-pmap case.
- **Failure scenario:** A driver probe running in a user process context calls ioremap(); pmap_enter switches CR3 to the kernel pmap, then a timer tick preempts it. On reschedule the CPU runs under the user process's pmap. pmap_enter resumes and writes the MMIO PTE into the user process's recursive-map page tables instead of the kernel pmap: the kernel mapping is never installed (later kernel access to the ioremap window faults/panics) and the user pmap is silently corrupted with a stray PTE.
- **Verifier:** The mechanism is real code: pmap_activate_for_update (sys/arch/i386/pmap.c:784-797) switches CR3/curpmap with no preempt_disable/intr_disable/lock; the timer IRQ preempts kernel mode whenever preempt_count==0 (sys/arch/i386/idt.c:250-262); and arch_switch_to (sys/arch/i386/sched.c:20-24) unconditionally restores next->proc->pmap on every switch, so a preempted pmap_enter resumes under the wrong CR3. However, the claimed failure scenario does not materialize with current call sites. (1) The cited ioremap case maps VAs 0xF7000000-0xF8000000 (ioremap.c:13-14) = PDEs 988-991, which lie inside the …

### UDF inline write: ext_attr_length underflow + offset+size overflow → heap OOB write
- **Location:** `sys/fs/udf/udf_write.c:692`  ·  integer-overflow  ·  verdict **PLAUSIBLE**
- **Subsystem:** minix/udf/9p/sysv/procfs
- **Defect:** In udf_write_file the inline (embedded-data) branch guards with `total_needed <= UDF_SECTOR_SIZE - sizeof(struct udf_fe) - disk_fe->ext_attr_length - 40`. All terms are unsigned; disk_fe->ext_attr_length is an untrusted uint32 read from the on-disk FE. When ext_attr_length > ~1832 the RHS underflows to a huge value so the guard always passes. It then computes alloc_area = sector_buf + sizeof(udf_fe) + disk_fe->ext_attr_length and does memcpy(alloc_area + offset, data, size) into the 2048-byte sector_buf. Additionally total_needed = offset + size (line 687) can wrap.
- **Failure scenario:** Write to a UDF file whose FE has AD_INLINE flag and ext_attr_length = 0x00100000. The bound underflows, the check passes, alloc_area points ~1 MB past the 2048-byte sector_buf, and the memcpy writes caller data far out of bounds → kernel heap overflow.
- **Verifier:** The arithmetic claim is exactly correct. At sys/fs/udf/udf_write.c:692 the guard is `total_needed <= UDF_SECTOR_SIZE - sizeof(struct udf_fe) - disk_fe->ext_attr_length - 40`. UDF_SECTOR_SIZE (2048, int) is promoted to unsigned when combined with sizeof (size_t), so the whole RHS is computed in unsigned 32-bit. `disk_fe->ext_attr_length` is a uint32 read straight off disk (line 686 `disk_fe = (struct udf_fe *)sector_buf`; sector_buf was just filled by fs->device->read at line 681) — fully untrusted. I compiled the packed struct: sizeof(struct udf_fe)=176, so the RHS is `2048 - 176 - ext_attr_le…

### sched_yield pick loop walks allthread without tid_lock (SMP UAF)
- **Location:** `sys/pm/sched.c:415`  ·  memory-safety  ·  verdict **PLAUSIBLE**
- **Subsystem:** power/pm subsystem
- **Defect:** The thread-selection scan in sched_yield() (lines 393-485) traverses the global allthread list protected only by local intr_disable(). The file's own KERN-06 comment (line 103) states that any registry walk must hold tid_lock because sched_reap_thread() unlinks and frees thread_t under that lock — and sched_tick() and sched_wakeup_n() were duly converted to thread_registry_lock() — but the hottest walker, the sched_yield pick loop, was not. Disabling local interrupts does not stop another CPU from reaping.
- **Failure scenario:** CPU0 is mid-scan holding a cursor t into allthread; CPU1 concurrently reaps a zombie (wait4 → sched_reap_process_threads, or sched_reap_any_zombie_sibling / detached-LWP reaper), which unlinks t and kfree()s it. CPU0 then reads t->state / t->t_allthread_next from freed (possibly reallocated) memory — it can follow a wild next pointer or context-switch into a freed thread → triple fault under fork/exit churn on multi-core.
- **Verifier:** The structural claim is accurate, but the SMP failure scenario is unreachable in the current tree, so I cannot confirm it as described.

What the auditor got right (verified by reading sys/pm/sched.c): (1) sched_yield()'s pick loop (lines 393-485) walks allthread and dereferences t->state / t->t_allthread_next protected only by intr_disable() at line 377 — it never takes tid_lock. (2) The file's own KERN-06 comment (lines 102-114) states registry walkers must hold tid_lock because sched_reap_thread() (line 891-895) unlinks and kfree()s thread_t under that lock. (3) Other walkers were duly conv…

### runqueue_remove recomputes level from mutable priority -> wrong-queue list corruption
- **Location:** `sys/kern/runqueue.c:110`  ·  memory-safety  ·  verdict **PLAUSIBLE**
- **Subsystem:** scheduler & SMP
- **Defect:** runqueue_remove() (and thus runqueue_pop, sched_dequeue, sched_steal_thread) recomputes the queue level with runqueue_level_for_thread(t) from the thread's CURRENT priority/sched_class. But a thread's priority, base_priority, and even sched_class are mutated IN PLACE after it is linked, without dequeue/re-enqueue: sched_decay.c decay_thread_priority()/sched_recalc_priority()/boost_starved_thread() rewrite t->priority, and futex priority inheritance (futex.c:990 sched_set_priority -> pm/sched.c:560) rewrites t->priority AND t->sched_class of a lock owner. If the level computed at removal differs from the level the node is physically linked in, runqueue_remove operates on the wrong runqueue_level_t: it updates the wrong queue's head/tail (e.g. sets queues[L2].head = t->rq_next while t was head of L1, leaving L1->head dangling at the removed node), decrements the wrong q->count (possible underflow), and clears the wrong bitmap bit. The originating queue is left with a stale head/tail pointing at a node sched_reap_thread() may kfree, so a later runqueue_peek() dereferences freed/reused storage (UAF), or a stranded bitmap bit points at an empty list.
- **Failure scenario:** Thread T is enqueued at TS priority 20 (level L1). Before it is popped, sched_decay_tick() raises T->priority for CPU usage (or a futex PI boost changes T->sched_class to RT). idle_balance/steal/dequeue calls runqueue_remove(rq,T), which computes L2 != L1 and corrupts both L1 (stale head) and L2 (bad count/bitmap). Next runqueue_peek() returns the stale L1 head -> use-after-free / kernel fault. Latent today because the runqueue population path (sched_enqueue via migrate/balance) is not wired into normal dispatch, but the in-place-priority-mutation vs level-index invariant is genuinely broken.
- **Verifier:** The mechanics of the claim are accurate: sys/kern/runqueue.c:110 runqueue_remove() recomputes the level via runqueue_level_for_thread(t) from the thread's CURRENT sched_class/priority and applies the head/tail unlink, count decrement, and bitmap_clear to that level — so if priority/class changed after runqueue_add, the wrong runqueue_level_t is corrupted exactly as described (stale head/tail in the real level, wrong count, wrong bitmap bit). A live in-place mutator exists: sched_set_priority (sys/pm/sched.c:560) rewrites t->sched_class/priority/base_priority with no requeue and no runqueue loc…

### bdf_parse: 32-bit multiply overflow in total_glyph_size -> undersized kmalloc -> heap overflow
- **Location:** `sys/drivers/video/bdf_pcf.c:298`  ·  integer-overflow  ·  verdict **PLAUSIBLE**
- **Subsystem:** video & console
- **Defect:** bdf_parse derives num_chars, bb_width, bb_height from the text header via parse_int() (bdf_pcf.c:234-248), which applies no range or overflow guard. It then computes row_bytes=(bb_width+7)/8, bytesperglyph=row_bytes*bb_height, and total_glyph_size=(size_t)num_chars*bytesperglyph (bdf_pcf.c:296-298). On i386 size_t is 32-bit, so this product can wrap. kmalloc(total_glyph_size) then returns a buffer far smaller than the per-glyph writes require. The second pass writes glyph rows at `glyph_data + (size_t)glyph_idx*bytesperglyph + bitmap_row*row_bytes` (bdf_pcf.c:317) for each STARTCHAR/BITMAP/ENDCHAR encountered in the file, overrunning the undersized allocation. (Parser is exported but not yet on a live untrusted-load path.)
- **Failure scenario:** A BDF with `FONTBOUNDINGBOX 8 16 0 0` (bytesperglyph=16) and `CHARS 268435456` (0x10000000). total_glyph_size = 0x10000000*16 = 0x100000000 truncates to 0 on 32-bit; kmalloc(0) returns a tiny/NULL-adjacent buffer. The file then supplies several BITMAP...ENDCHAR blocks; each ENDCHAR increments glyph_idx and each bitmap row memcpy-writes into glyph_data at multiples of 16 bytes, corrupting kernel heap past the allocation.
- **Verifier:** The 32-bit multiply-overflow arithmetic claim is real: on i386 size_t is 32-bit and bdf_pcf.c:298 computes total_glyph_size=(size_t)num_chars*bytesperglyph from unbounded header values (parse_int() at 234-248 has no range guard; the only check at 292 is a zero-check). So the product genuinely wraps mod 2^32. HOWEVER, the finding's specific failure scenario is factually wrong and defused: FONTBOUNDINGBOX 8 16 + CHARS 268435456 gives 0x10000000*16 = 0x100000000 which truncates to exactly 0, and kmalloc rejects zero — vm_kmem.c:93 `if (size == 0) return NULL;` — so bdf_pcf.c:300-301 returns -1. T…

### pcf_parse: uint32 overflow of offset+constant bypasses table bounds checks -> wild-pointer OOB read
- **Location:** `sys/drivers/video/bdf_pcf.c:91`  ·  integer-overflow  ·  verdict **PLAUSIBLE**
- **Subsystem:** video & console
- **Defect:** pcf_parse reads table offsets straight from the TOC (attacker-controlled uint32) and bounds-checks them with `metrics_offset + 8 > size` (bdf_pcf.c:91), `metrics_offset + 6 + 5 > size` (113), `metrics_offset + 8 + 12 > size` (127) and `bitmaps_offset + 8 > size` (153), all computed in 32-bit unsigned arithmetic. An offset near UINT32_MAX makes offset+constant wrap to a small value that passes the check, after which `mp = base + metrics_offset + 4` / `bp = base + bitmaps_offset + 4` form pointers far outside the buffer that are then dereferenced. The later `(uintptr_t)(bp + bitmap_size) > (uintptr_t)(base + size)` check (176) can likewise be defeated by a bitmap_size that wraps the pointer past the address-space top. (Parser exported but not yet on a live untrusted-load path.)
- **Failure scenario:** A PCF whose METRICS TOC entry has offset=0xFFFFFFFC and size>4: `metrics_offset + 8` wraps to 4, `4 > size` is false, so the check passes; `mp = base + 0xFFFFFFFC + 4` = base (wrapped) or a wild address, and pcf_read16/32(mp,...) dereferences it -> OOB read / fault.
- **Verifier:** The integer-overflow claim is technically correct. On i386 (-m32) size_t is 32-bit. In bdf_pcf.c the TOC offset fields are raw uint32_t (struct pcf_toc_entry.offset, bdf_pcf.h:47) copied straight from attacker data (metrics_offset = metrics_entry->offset, line 86; bitmaps_offset = bitmaps_entry->offset, line 147). The bounds checks metrics_offset + 8 > size (91), metrics_offset + 6 + 5 > size (113), metrics_offset + 8 + 12 > size (127), bitmaps_offset + 8 > size (153) are all evaluated in 32-bit unsigned arithmetic, so an offset near UINT32_MAX wraps the sum to a small value that passes the ch…


## Refuted (false positives)

- **Out-of-bounds stack read of args[i+1] for trailing ARG_LONG in syscall trace** (`sys/arch/i386/syscall.c`) — The quoted code is real (sys/arch/i386/syscall.c:291 `uint32_t args[8]`, :330 loop bounded by fmt->nargs, :338 `((int64_t)args[i+1] << 32) | args[i]` with no i+1<8 guard), but the claimed trigger cannot occur. struct syscall_fmt (sys/exec/perso/personality.h:17-20) declares `int arg_types[6]` — only…
- **UDF truncate/write trust on-disk alloc_desc_length → OOB read/write** (`sys/fs/udf/udf_write.c`) — The finding's arithmetic observation is factually correct: udf_read_file validates `sizeof(udf_fe)+ext_attr_length+alloc_desc_length <= UDF_SECTOR_SIZE` (udf.c:303-310) before deriving num_ads, whereas udf_truncate_file (udf_write.c:1215, 1272, 1351, 1402), udf_process_short/long_ads, and udf_write_…
- **ELKS: multi-byte writes past DS segment after single-byte bounds check** (`sys/exec/perso/perso_elks.c`) — The arithmetic half of the claim is true: elks_ds_pointer() -> ldt_translate_selector_offset() (sys/include/sys/ldt.h:95) only checks `offset > limit`, i.e. validates a single byte, and the cited handlers (perso_elks.c:1516,1538,1560,1582 and the /dev/kmem ioctl GET* stores at 2310/2316/2322/2328/23…